\section{Probability Distributions}


\subsection{}
\label{2.1}
Let $x$ be a variable such that
%
\begin{equation}
p(x | \mu) = \mu ^ x (1 - \mu) ^ {1 - x},
\end{equation}
%
where $x \in \{ 0, 1 \}$.
Then,
%
\begin{equation}
\sum_{x} p(x | \mu) = 1.
\end{equation}
%
By the definition,
%
\begin{equation}
\begin{aligned}
{\rm E} x &= \mu, \\
{\rm E} x ^ 2 &= \mu,
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
{\rm var} x = {\rm E} x ^ 2 - \left( {\rm E} x \right) ^ 2,
\end{equation}
%
we have
%
\begin{equation}
{\rm var} x = \mu (1 - \mu).
\end{equation}
%
By the definition,
%
\begin{equation}
{\rm H} (x) = - \sum_{x} p(x | \mu) \ln p(x | \mu).
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm H} (x) = - \mu \ln \mu - (1 - \mu) \ln (1 - \mu).
\end{equation}
%


\subsection{}
\label{2.2}
Let $x$ be a variable such that
%
\begin{equation}
p(x | \mu) = \left( \frac{1 - \mu}{2} \right) ^ \frac{1 - x}{2} \left( \frac{1 + \mu}{2} \right) ^ \frac{1 + x}{2},
\end{equation}
%
where $x \in \{ -1, 1 \}$.
Then,
%
\begin{equation}
\sum_{x} p(x | \mu) = 1.
\end{equation}
%
By the definition,
%
\begin{equation}
\begin{aligned}
{\rm E} x &= \mu, \\
{\rm E} x ^ 2 &= 1,
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
{\rm var} x = {\rm E} x ^ 2 - \left( {\rm E} x \right) ^ 2,
\end{equation}
%
we have
%
\begin{equation}
{\rm var} x = 1 - \mu ^ 2.
\end{equation}
%
By the definition,
%
\begin{equation}
{\rm H} (x) = - \sum_{x} p(x | \mu) \ln p(x | \mu).
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm H} (x) = - \frac{1 - \mu}{2} \ln \frac{1 - \mu}{2} - \frac{1 + \mu}{2} \ln \frac{1 + \mu}{2}.
\end{equation}
%


\subsection{}
\label{2.3}
By the definition,
%
\begin{equation}
\begin{aligned}
{N \choose m} &= \frac{N!}{m! (N - m)!}, \\
{N \choose m - 1} &= \frac{N!}{(m - 1)! (N - m + 1)!}
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
{N \choose m} + {N \choose m - 1} = \frac{(N - m + 1) N! + m N!}{m! (N - m + 1)!}.
\end{equation}
%
By the definition, the right hand side can be written as
%
\begin{equation}
\frac{(N + 1)!}{m! (N + 1 - m)!} = {N + 1 \choose m}.
\end{equation}
%
Thus,
%
\begin{equation}
{N \choose m} + {N \choose m - 1} = {N + 1 \choose m}.
\end{equation}
%

Note that 
%
\begin{equation}
1 + x = \sum_{m = 0}^{1} {1 \choose m} x ^ m.
\end{equation}
%
Let us assume that 
%
\begin{equation}
(1 + x) ^ N = \sum_{m = 0}^{N} {N \choose m} x ^ m.
\end{equation}
%
Then,
%
\begin{equation}
(1 + x) ^ {N + 1} = \sum_{m = 0}^{N} {N \choose m} x ^ m + \sum_{m = 0}^{N} {N \choose m} x ^ {m + 1}.
\end{equation}
%
By the result above, the right hand side can be written as
%
\begin{equation}
\sum_{m = 0}^{N} {N \choose m} x ^ m + \sum_{m = 1}^{N + 1} {N \choose m - 1} x ^ m = 1 + x ^ {N + 1} + \sum_{m = 1}^{N} {N + 1 \choose m} x ^ m.
\end{equation}
%
Therefore,
%
\begin{equation}
(1 + x) ^ {N + 1} = \sum_{m = 0}^{N + 1} {N + 1 \choose m} x ^ m.
\end{equation}
%
Thus, the assumption is proved by induction on $N$.

Finally, let $m$ be a variable such that
%
\begin{equation}
p(m | \mu) = {N \choose m} \mu ^ m (1 - \mu) ^ {N - m}.
\end{equation}
%
Then
%
\begin{equation}
\sum_{m = 0}^{N} p(m | \mu) = \sum_{m = 0}^{N} {N \choose m} \mu ^ m (1 - \mu) ^ {N - m}.
\end{equation}
%
By the result above, the right hand side can be written as
%
\begin{equation}
(1 - \mu) ^ N \sum_{m = 0}^{N} {N \choose m} \left( \frac{\mu}{1 - \mu} \right) ^ m = (1 - \mu) ^ N \left( 1 + \frac{\mu}{1 - \mu} \right) ^ N.
\end{equation}
%
Therefore,
%
\begin{equation}
\sum_{m = 0}^{N} p(m | \mu) = 1.
\end{equation}
%


\subsection{}
\label{2.4}
Let $m$ be a variable such that
%
\begin{equation}
p(m | \mu) = {N \choose m} \mu ^ m (1 - \mu) ^ {N - m}.
\end{equation}
%
Then
%
\begin{equation}
{\rm E} m = \sum_{m = 0}^{N} m {N \choose m} \mu ^ m (1 - \mu) ^ {N - m}.
\end{equation}
%
Differentiating both sides of 
%
\begin{equation}
\sum_{m = 0}^{N} {N \choose m} \mu ^ m (1 - \mu) ^ {N - m}  = 1
\end{equation}
%
with respect to $\mu$ gives
%
\begin{equation}
\sum_{m = 0}^{N} m {N \choose m} \mu ^ {m - 1} (1 - \mu) ^ {N - m} - \sum_{m = 0}^{N} (N - m) {N \choose m} \mu ^ m (1 - \mu) ^ {N - m - 1} = 0.
\end{equation}
%
The first term of the left hand side can be written as $\frac{1}{\mu} {\rm E} m$.
Since
%
\begin{equation}
(N - m) {N \choose m} = N {N - 1 \choose m},
\end{equation}
%
the second term of the left hand side can be written as
%
\begin{equation}
- N \sum_{m = 0}^{N - 1} {N - 1 \choose m} \mu ^ m (1 - \mu) ^ {N - m - 1} = - N.
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm E} m = N \mu. 
\end{equation}
%

Differentiating both sides of 
%
\begin{equation}
\sum_{m = 0}^{N} {N \choose m} \mu ^ m (1 - \mu) ^ {N - m}  = 1
\end{equation}
%
twice with respect to $\mu$ gives
%
\begin{equation}
\begin{aligned}
&\sum_{m = 0}^{N} m (m - 1) {N \choose m} \mu ^ {m - 2} (1 - \mu) ^ {N - m} \\
&- 2 \sum_{m = 0}^{N} m (N - m) {N \choose m} \mu ^ {m - 1} (1 - \mu) ^ {N - m - 1} \\
&+ \sum_{m = 0}^{N} (N - m) (N - m - 1) {N \choose m} \mu ^ m (1 - \mu) ^ {N - m - 2} = 0.
\end{aligned}
\end{equation}
%
The first term of the left hand side can be written as $\frac{1}{\mu ^ 2} {\rm E} m (m - 1)$.
Since
%
\begin{equation}
\begin{aligned}
m (N - m) {N \choose m} &= N (N - 1) {N - 2 \choose m - 1}, \\
(N - m) (N - m - 1) {N \choose m} &= N (N - 1) {N - 2 \choose m},
\end{aligned}
\end{equation}
%
the second and third term of the left hand side can be written as
%
\begin{equation}
\begin{aligned}
- 2 N (N - 1) \sum_{m = 1}^{N - 1} {N - 2 \choose m - 1} \mu ^ {m - 1} (1 - \mu) ^ {N - m - 1} &= - 2 N (N - 1), \\
N (N - 1) \sum_{m = 0}^{N} {N - 2 \choose m} \mu ^ m (1 - \mu) ^ {N - m - 2} &= N (N - 1).
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm E} m (m - 1) = N (N - 1) \mu ^ 2.
\end{equation}
%
Thus, since 
%
\begin{equation}
{\rm var} m = {\rm E} m (m - 1) + {\rm E} m - ( {\rm E} m ) ^ 2,
\end{equation}
%
we have
%
\begin{equation}
{\rm var} m = N \mu ( 1 - \mu).
\end{equation}
%


\subsection{}
\label{2.5}
By the definition,
%
\begin{equation}
\Gamma (a) \Gamma (b) = \int_{0}^{\infty} x ^ {a - 1} \exp (- x) dx \int_{0}^{\infty} y ^ {b - 1} \exp (- y) dy.
\end{equation}
%
By the transformation $t = x + y$, the right hand side can be written as
%
\begin{equation}
\begin{aligned}
&\int_{0}^{\infty} x ^ {a - 1} \left( \int_{x}^{\infty} (t - x) ^ {b - 1} \exp (- t) dt \right) dx \\
&=  \int_{0}^{\infty} \left( \int_{0}^{t} x ^ {a - 1} (t - x) ^ {b - 1} dx \right) \exp (- t) dt.
\end{aligned}
\end{equation}
%
By the transformation $x = t \mu$, the right hand side can be written as
%
\begin{equation}
\begin{aligned}
&\int_{0}^{\infty} \left( \int_{0}^{1} (t \mu) ^ {a - 1} t ^ {b - 1} (1 - \mu) ^ {b - 1} t d\mu \right) \exp (- t) dt \\
&= \int_{0}^{1} \mu ^ {a - 1} (1 - \mu) ^ {b - 1} d\mu \int_{0}^{\infty} t ^ {a + b - 1} \exp ( - t ) dt.
\end{aligned}
\end{equation}
%
By the definition, the second integral of the right hand side  can be written as $\Gamma(a + b)$.
%
Therefore,
%
\begin{equation}
\int_{0}^{1} \mu ^ {a - 1} (1 - \mu) ^ {b - 1} d\mu = \frac{\Gamma (a) \Gamma (b)}{\Gamma (a + b)}.
\end{equation}
%


\subsection{}
\label{2.6}
Let $\mu$ be a variable such that
%
\begin{equation}
p(\mu | a, b) = \frac{\Gamma(a + b)}{\Gamma(a) \Gamma(b)} \mu ^ {a - 1} (1 - \mu) ^ {b - 1}.
\end{equation}
%
Then
%
\begin{equation}
\begin{aligned}
{\rm E} \mu &= \frac{\Gamma(a + b)}{\Gamma(a) \Gamma(b)} \int_{0}^{1} \mu ^ a (1 - \mu) ^ {b - 1} d\mu, \\
{\rm E} \mu ^ 2 &= \frac{\Gamma(a + b)}{\Gamma(a) \Gamma(b)} \int_{0}^{1} \mu ^ {a + 1} (1 - \mu) ^ {b - 1} d\mu.
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
\begin{aligned}
\int_{0}^{1} \mu ^ a (1 - \mu) ^ {b - 1} d\mu &= \frac{\Gamma (a + 1) \Gamma (b)}{\Gamma (a + b + 1)}, \\
\int_{0}^{1} \mu ^ {a + 1} (1 - \mu) ^ {b - 1} d\mu &= \frac{\Gamma (a + 2) \Gamma (b)}{\Gamma (a + b + 2)},
\end{aligned}
\end{equation}
%
we have
%
\begin{equation}
\begin{aligned}
{\rm E} \mu &= \frac{a}{a + b}, \\
{\rm E} \mu ^ 2 &= \frac{a (a + 1)}{(a + b) (a + b + 1)}.
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
{\rm var} \mu = {\rm E} \mu ^ 2 - \left( {\rm E \mu} \right) ^ 2,
\end{equation}
%
we have
%
\begin{equation}
{\rm var} \mu = \frac{a b}{(a + b) ^ 2 (a + b + 1)}.
\end{equation}
%
Since
%
\begin{equation}
\frac{\partial}{\partial \mu} p( \mu | a, b) = \frac{\Gamma(a + b)}{\Gamma(a) \Gamma(b)} \mu ^ {a - 1} (1 - \mu) ^ {b - 1} \left( \frac{a - 1}{\mu} - \frac{b - 1}{1 - \mu} \right),
\end{equation}
%
we have
%
\begin{equation}
{\rm mode} \mu = \frac{a - 1}{a + b - 2}.
\end{equation}
%


\subsection{}
Let $m$ and $l$ be a variable such that
%
\begin{equation}
p(m, l | \mu) = {m + l \choose m} \mu ^ m (1 - \mu) ^ l,
\end{equation}
%
where
%
\begin{equation}
p(\mu | a, b) = \frac{\Gamma(a + b)}{\Gamma(a) \Gamma(b)} \mu ^ {a - 1} (1 - \mu) ^ {b - 1}.
\end{equation}
%
By \ref{2.6}, 
%
\begin{equation}
{\rm E} (\mu | a, b) = \frac{a}{a + b}.
\end{equation}
%
Note that
%
\begin{equation}
\mu_{\rm ML} = \frac{m}{m + l}.
\end{equation}
%
Since
%
\begin{equation}
p(\mu | m, l, a,  b) \propto p(m, l | \mu) p(\mu | a, b),
\end{equation}
%
we have
%
\begin{equation}
p(\mu | m, l, a,  b) = \frac{\Gamma(m + l + a + b)}{\Gamma (m + a) \Gamma (l + b)} \mu ^ {m + a - 1} (1 - \mu) ^ {l + b - 1}.
\end{equation}
%
Therefore, by \ref{2.6}, 
%
\begin{equation}
{\rm E} (\mu | m, l, a, b) = \frac{m + a}{m + l + a + b}.
\end{equation}
%
Thus,
%
\begin{equation}
{\rm E} (\mu | m, l, a, b) = \lambda \mu_{\rm ML} + (1 - \lambda) {\rm E} (\mu | a, b),
\end{equation}
%
where
%
\begin{equation}
\lambda = \frac{m + l}{m + l + a + b}.
\end{equation}
%


\subsection{(Incomplete)}
\label{2.8}
Let $x$ and $y$ be variables.
Then, by the definition,
%
\begin{equation}
{\rm E} x = \int x p(x) dx.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\int x \left( \int p(x, y) dy \right) dx = \int \left( \int x p(x | y) dx \right) p(y) dy.
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm E} x = {\rm E}_y \left( {\rm E}_x (x | y) \right).
\end{equation}
%

By the definition,
%
\begin{equation}
{\rm var} x = {\rm E} \left( x - {\rm E} x \right) ^ 2.
\end{equation}
%
By the result above, the right hand side can be written as
%
\begin{equation}
\begin{aligned}
&{\rm E}_y \left( {\rm E}_x \left( \left( x - {\rm E}_x (x | y) + {\rm E}_x (x | y) - {\rm E} x \right) ^ 2 | y \right) \right) \\
&= {\rm E}_y \left( {\rm E}_x \left( \left( x - {\rm E}_x (x | y) \right) ^ 2 | y \right) \right) \\
&+ 2 {\rm E}_y \left( {\rm E}_x \left( \left( x - {\rm E}_x (x | y) \right) \left( {\rm E}_x (x | y) - {\rm E} x \right) | y \right) \right) \\
&+ {\rm E}_y \left( {\rm E}_x \left( \left( {\rm E}_x (x | y) - {\rm E} x \right) ^ 2 | y \right) \right)
\end{aligned}
\end{equation}
%
Let us look at each term of the right hand side.
By the definition, the first term can be written as ${\rm E}_y \left( {\rm var}_x (x | y) \right)$.
The second term can be written as
%
\begin{equation}
2 {\rm E}_y \left( \left( {\rm E}_x (x | y) - {\rm E} x \right) {\rm E}_x \left( \left( x - {\rm E}_x (x | y) \right) | y \right) \right)
\end{equation}
%
By the result above, the third term can be written as
%
\begin{equation}
{\rm E}_y \left( {\rm E}_x (x | y) - {\rm E}_y \left( {\rm E}_x (x | y) \right) \right) ^ 2 = {\rm var}_y \left( {\rm E}_x (x | y) \right).
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm var} x = {\rm E}_y \left( {\rm var}_x (x | y) \right) + {\rm var}_y \left( {\rm E}_x (x | y) \right).
\end{equation}
%


\subsection{(Incomplete)}
\label{2.9}
For a vector $\bm{\mu}$ in 2 dimensions, \ref{2.5} gives
%
\[
\int_{
\begin{subarray}{l}
\mu_1 + \mu_2 = 1 \\\\
\mu_1 \geq 0, \mu_2 \geq 0
\end{subarray}
} 
\mu_1 ^ {\alpha_1 - 1} \mu_2 ^ {\alpha_2 - 1} d\bm{\mu}
= \frac{\Gamma(\alpha_1) \Gamma(\alpha_2)}{\Gamma(\alpha_1 + \alpha_2)}.
\]
%
For a vector $\bm{\mu}$ in $M$ dimensions, let us assume that
%
\[ 
\int_{
\begin{subarray}{l}
\sum_{k = 1}^{M} \mu_k = 1 \\\\
\mu_k \geq 0
\end{subarray}
} 
\prod_{k = 1}^{M} \mu_k ^ {\alpha_k - 1} d\bm{\mu} 
= \frac{\prod_{k = 1}^{M} \Gamma(\alpha_k)}{\Gamma \left( \sum_{k = 1}^{M} \alpha_k \right)}.
\]
%
Then, for a vector $\bm{\mu}$ in $M + 1$ dimensions, 
%
\[
\int_{
\begin{subarray}{l}
\sum_{k = 1}^{M + 1} \mu_k = 1 \\\\
\mu_k \geq 0
\end{subarray}
}
\prod_{k = 1}^{M + 1} \mu_k ^ {\alpha_k - 1} d\bm{\mu} 
= \int_{0}^{1} \mu_{M + 1} ^ {\alpha_{M + 1} - 1} 
\left( \int_{
\begin{subarray}{l}
\sum_{k = 1}^{M} \mu'_k = 1 - \mu_{M + 1} \\\\
\mu'_k \geq 0
\end{subarray}
}
\prod_{k = 1}^{M} {\mu'_k} ^ {\alpha_k - 1} d\bm{\mu}' 
\right) d\mu_{M + 1}.
\]
%
where $\bm{\mu}'$ is the vector of the first $M$ elements of $\bm{\mu}$.
By the transformation 
%
\begin{equation}
\bm{\mu}'' = \frac{1}{1 - \mu_{M + 1}} \bm{\mu}',
\end{equation}
%
the right hand side can be written as
%
\[
\int_{0}^{1} \mu_{M + 1} ^ {\alpha_{M + 1} - 1} 
\left( \int_{
\begin{subarray}{l}
\sum_{k = 1}^{M} \mu''_k = 1 \\\\
\mu''_k \geq 0
\end{subarray}
} \left( \prod_{k = 1}^{M} \left( (1 - \mu_{M + 1}){\mu''_k} \right) ^ {\alpha_k - 1} \right) (1 - \mu_{M + 1}) ^ M d\bm{\mu}'' \right) d\mu_{M + 1},
\]
%
so that
\[
\int_{0}^{1} \mu_{M + 1} ^ {\alpha_{M + 1} - 1} (1 - \mu_{M + 1}) ^ {\Sigma_{k = 1}^{M} \alpha_k} 
\left( \int_{
\begin{subarray}{l}
\sum_{k = 1}^{M} \mu''_k = 1 \\\\
\mu''_k \geq 0
\end{subarray}
} \prod_{k = 1}^{M} {\mu''_k} ^ {\alpha_k - 1} d\bm{\mu}'' \right) d\mu_{M + 1}.
\]
By the assumption, it can be written as
%
\begin{equation}
\frac{\prod_{k = 1}^{M} \Gamma(\alpha_k)}{\Gamma \left( \sum_{k = 1}^{M} \alpha_k \right)} \frac{\Gamma(\alpha_{M + 1}) \Gamma \left( \sum_{k = 1}^{M} \alpha_k + 1 \right)}{\Gamma \left( \sum_{k = 1}^{M + 1} \alpha_k + 1 \right)}
= \frac{\sum_{k = 1}^{M} \alpha_k}{\sum_{k = 1}^{M + 1} \alpha_k} \frac{\prod_{k = 1}^{M + 1} \Gamma(\alpha_k)}{\Gamma \left( \sum_{k = 1}^{M + 1} \alpha_k \right)}.
\end{equation}
%
Therefore,
%
\begin{equation}
\int \prod_{k = 1}^{M + 1} \mu_k ^ {\alpha_k - 1} d\bm{\mu} = \frac{\prod_{k = 1}^{M + 1} \Gamma(\alpha_k)}{\Gamma \left( \sum_{k = 1}^{M + 1} \alpha_k \right)} ?
\end{equation}
%
Thus, the assumption is proved by induction on $M$.


\subsection{}
\label{2.10}
Let $\bm{\mu}$ be a variable such that
%
\begin{equation}
p(\bm{\mu} | \bm{\alpha}) = \frac{\Gamma \left( \sum_{k = 1}^{K} \alpha_k \right)}{\prod_{k = 1}^{K} \Gamma(\alpha_k)} \prod_{k = 1}^{K} \mu_k ^ {\alpha_k - 1}.
\end{equation}
%
Then
%
\begin{equation}
\begin{aligned}
{\rm E} \mu_j &= \int \mu_j p(\bm{\mu} | \bm{\alpha}) d\bm{\mu}, \\
{\rm E} \mu_j ^ 2 &= \int \mu_j ^ 2 p(\bm{\mu} | \bm{\alpha}) d\bm{\mu}, \\
{\rm E} \mu_j \mu_l &= \int \mu_j \mu_l p(\bm{\mu} | \bm{\alpha}) d\bm{\mu}.
\end{aligned}
\end{equation}
%
If $j \neq l$, then the right hand sides can be written as
%
\begin{equation}
\begin{aligned}
\frac{\Gamma \left( \sum_{k = 1}^{K} \alpha_k \right)}{\prod_{k = 1}^{K} \Gamma(\alpha_k)} \frac{\frac{\Gamma(\alpha_j + 1)}{\Gamma(\alpha_j)} \prod_{k = 1}^{K} \Gamma(\alpha_k)}{\Gamma \left( \sum_{k = 1}^{K} \alpha_k + 1 \right)} &= \frac{\alpha_j}{\sum_{k = 1}^{K} \alpha_k}, \\
\frac{\Gamma \left( \sum_{k = 1}^{K} \alpha_k \right)}{\prod_{k = 1}^{K} \Gamma(\alpha_k)} \frac{\frac{\Gamma(\alpha_j + 2)}{\Gamma(\alpha_j)} \prod_{k = 1}^{K} \Gamma(\alpha_k)}{\Gamma \left( \sum_{k = 1}^{K} \alpha_k + 2 \right)} &= \frac{\alpha_j (\alpha_j + 1)}{\sum_{k = 1}^{K} \alpha_k \left( \sum_{k = 1}^{K} \alpha_k + 1 \right)},  \\
\frac{\Gamma \left( \sum_{k = 1}^{K} \alpha_k \right)}{\prod_{k = 1}^{K} \Gamma(\alpha_k)} \frac{\frac{\Gamma(\alpha_j + 1) \Gamma(\alpha_l + 1)}{\Gamma(\alpha_j) \Gamma(\alpha_l)} \prod_{k = 1}^{K} \Gamma(\alpha_k)}{\Gamma \left( \sum_{k = 1}^{K} \alpha_k + 2 \right)} &= \frac{\alpha_j \alpha_l}{\sum_{k = 1}^{K} \alpha_k \left( \sum_{k = 1}^{K} \alpha_k + 1 \right)}.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
\begin{aligned}
{\rm E} \mu_j &= \frac{\alpha_j}{\sum_{k = 1}^{K} \alpha_k}. \\
{\rm E} \mu_j ^ 2 &= \frac{\alpha_j (\alpha_j + 1)}{\sum_{k = 1}^{K} \alpha_k \left( \sum_{k = 1}^{K} \alpha_k + 1 \right)}, \\
{\rm E} \mu_j \mu_l &= \frac{\alpha_j \alpha_l}{\sum_{k = 1}^{K} \alpha_k \left( \sum_{k = 1}^{K} \alpha_k + 1 \right)}.
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
\begin{aligned}
{\rm var} \mu_j &= {\rm E} \mu_j ^ 2 - \left( {\rm E} \mu_j \right) ^ 2, \\
{\rm cov} \left( \mu_j, \mu_l \right) &= {\rm E} \mu_j \mu_l - {\rm E} \mu_j {\rm E} \mu_l,
\end{aligned}
\end{equation}
%
we have
%
\begin{equation}
\begin{aligned}
{\rm var} \mu_j &= \frac{\alpha_j \left( \sum_{k = 1}^{K} \alpha_k - \alpha_j \right)}{\left( \sum_{k = 1}^{K} \alpha_k \right) ^ 2 \left( \sum_{k = 1}^{K} \alpha_k + 1 \right)}, \\
{\rm cov} \left( \mu_j, \mu_l \right) &= - \frac{\alpha_j \alpha_l}{\left( \sum_{k = 1}^{K} \alpha_k \right) ^ 2 \left( \sum_{k = 1}^{K} \alpha_k + 1 \right)}.
\end{aligned}
\end{equation}
%


\subsection{}
\label{2.11}
Let $\bm{\mu}$ be a variable such that
%
\begin{equation}
p(\bm{\mu} | \bm{\alpha}) = \frac{\Gamma \left( \sum_{k = 1}^{K} \alpha_k \right)}{\prod_{k = 1}^{K} \Gamma(\alpha_k)} \prod_{k = 1}^{K} \mu_k ^ {\alpha_k - 1}.
\end{equation}
%
Then
%
\begin{equation}
{\rm E} \ln \mu_j = \int \left( \ln \mu_j \right) p(\bm{\mu} | \bm{\alpha}) d\bm{\mu}.
\end{equation}
%
Since
%
\begin{equation}
\frac{\partial}{\partial \alpha_j} p(\bm{\mu} | \bm{\alpha}) = \left( \frac{\Gamma' \left( \sum_{k = 1}^{K} \alpha_k \right)}{\Gamma \left( \sum_{k = 1}^{K} \alpha_k \right)} - \frac{\Gamma'(\alpha_j)}{\Gamma(\alpha_j)} + \ln \mu_j \right) p(\bm{\mu} | \bm{\alpha}),
\end{equation}
%
we have
%
\begin{equation}
{\rm E} \ln \mu_j = \frac{\partial}{\partial \alpha_j} \int p(\bm{\mu} | \bm{\alpha}) d\bm{\mu} + \left( \psi (\alpha_j) - \psi \left( \sum_{k = 1}^{K} \alpha_k \right) \right) \int p(\bm{\mu} | \bm{\alpha}) d\bm{\mu},
\end{equation}
%
where
%
\begin{equation}
\psi(a) = \frac{d}{da} \ln \Gamma(a).
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm E} \ln \mu_j = \psi(\alpha_j) - \psi \left( \sum_{k = 1}^{K} \alpha_k \right).
\end{equation}
%


\subsection{}
\label{2.12}
Let $x$ be a variable such that
%
\begin{equation}
p(x | a,  b) = \frac{1}{b - a},
\end{equation}
%
where $a < b$.
Then
%
\begin{equation}
\int_{a}^{b} p(x | a, b) dx = 1.
\end{equation}
%
Note that
%
\begin{equation}
\begin{aligned}
{\rm E} x &= \int_{a}^{b} x p(x | a, b) dx, \\
{\rm E} x ^ 2 &= \int_{a}^{b} x ^ 2 p(x | a, b) dx.
\end{aligned}
\end{equation}
%
The right hand sides can be written as
%
\begin{equation}
\begin{aligned}
\frac{1}{b - a} \int_{a}^{b} x dx &= \frac{1}{2} (a + b), \\
\frac{1}{b - a} \int_{a}^{b} x ^ 2 dx &= \frac{1}{3} \left( a ^ 2 + a b + b ^ 2 \right).
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
\begin{aligned}
{\rm E} x &= \frac{1}{2} (a + b), \\
{\rm E} x ^ 2 &= \frac{1}{3} \left( a ^ 2 + a b + b ^ 2 \right).
\end{aligned}
\end{equation}
%
Since 
%
\begin{equation}
{\rm var} x = {\rm E} x ^ 2 - \left( {\rm E} x \right) ^ 2,
\end{equation}
%
we have
%
\begin{equation}
{\rm var} x = \frac{1}{12} (b - a) ^ 2.
\end{equation}
%


\subsection{}
\label{2.13}
Let $\mathbf{x}$ be a variable in $D$ dimensions and
%
\begin{equation}
\begin{aligned}
p(\mathbf{x}) &= \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}), \\
q(\mathbf{x}) &= \mathcal{N} (\mathbf{x} | \mathbf{m}, \mathbf{L}).
\end{aligned}
\end{equation}
%
Then, by the definition,
%
\begin{equation}
{\rm KL} (p || q) = - \int \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) \ln \frac{\mathcal{N} (\mathbf{x} | \mathbf{m}, \mathbf{L})}{\mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma})} d\mathbf{x}.
\end{equation}
%
Since
%
\begin{equation}
\ln \frac{\mathcal{N} (\mathbf{x} | \mathbf{m}, \mathbf{L})}{\mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma})} = \ln \frac{(2 \pi) ^ {- \frac{D}{2}} \left( \abs{\det \mathbf{L}} \right) ^ {- \frac{1}{2}} \exp \left( - \frac{1}{2} (\mathbf{x} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} (\mathbf{x} - \mathbf{m}) \right)}{(2 \pi) ^ {- \frac{D}{2}} \left( \abs{\det \bm{\Sigma}} \right) ^ {- \frac{1}{2}} \exp \left( - \frac{1}{2} (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) \right)},
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\begin{aligned}
&\frac{1}{2} \ln \abs{\frac{\det \mathbf{L}}{\det \bm{\Sigma}}} \int \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} \\
&+ \frac{1}{2} \int (\mathbf{x} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} (\mathbf{x} - \mathbf{m}) \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} \\
&- \frac{1}{2} \int (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x}.
\end{aligned}
\end{equation}
%
Let us look at each term.
Since
%
\begin{equation}
\int \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} = 1,
\end{equation}
%
the first term can be written as $\frac{1}{2} \ln \abs{\frac{\det \mathbf{L}}{\det \bm{\Sigma}}}$.
Since
%
\begin{equation}
(\mathbf{x} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} (\mathbf{x} - \mathbf{m}) = (\mathbf{x} - \bm{\mu} + \bm{\mu} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} (\mathbf{x} - \bm{\mu} + \bm{\mu} - \mathbf{m}),
\end{equation}
%
the second term can be written as
%
\begin{equation}
\begin{aligned}
&\frac{1}{2} \int (\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{L} ^ {- 1} (\mathbf{x} - \bm{\mu}) \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} \\
&+ (\bm{\mu} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} \int (\mathbf{x} - \bm{\mu}) \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} \\
&+ \frac{1}{2} (\bm{\mu} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} (\bm{\mu} - \mathbf{m}) \int \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x}.
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
\begin{aligned}
\int \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} &= 1, \\
\int \mathbf{x} \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} &= \bm{\mu}, \\
\int (\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} &= \bm{\Sigma},
\end{aligned}
\end{equation}
%
it can be written as
%
\begin{equation}
\frac{1}{2} \tr \left( \mathbf{L} ^ {- 1} \bm{\Sigma} \right)
+ \frac{1}{2} (\bm{\mu} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} (\bm{\mu} -  \mathbf{m}).
\end{equation}
%
Since
%
\begin{equation}
\int (\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}) d\mathbf{x} = \bm{\Sigma},
\end{equation}
%
the third term can be written as
%
\begin{equation}
- \frac{1}{2} \tr \left( \bm{\Sigma} ^ {- 1} \bm{\Sigma} \right) = - \frac{D}{2}
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm KL} (p || q) 
= \frac{1}{2} \left( \ln \abs{\frac{\det \mathbf{L}}{\det \bm{\Sigma}}} 
+ \tr \left( \mathbf{L} ^ {- 1} \bm{\Sigma} \right) 
+ (\bm{\mu} - \mathbf{m}) ^ \intercal \mathbf{L} ^ {- 1} (\bm{\mu} - \mathbf{m}) 
- D \right).
\end{equation}
%


\subsection{}
\label{2.14}
Let $\mathbf{x}$ be a variable in $D$ dimensions and 
%
\begin{equation}
\begin{aligned}
L(p(\mathbf{x})) = 
&- \int p(\mathbf{x}) \ln p(\mathbf{x}) d\mathbf{x}
+ \lambda \left( \int p(\mathbf{x}) d\mathbf{x} - 1 \right) \\
&+ \mathbf{l} ^ \intercal \left( \int \mathbf{x} p(\mathbf{x}) d\mathbf{x} - \bm{\mu} \right)
+ \mathbf{m} ^ \intercal \left( \int (\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal p(\mathbf{x}) d\mathbf{x} - \bm{\Sigma} \right) \mathbf{m}.
\end{aligned}
\end{equation}
%
Then
%
\begin{equation}
\frac{\delta L(p(\mathbf{x}))}{\delta p(\mathbf{x})} 
= - \ln p(\mathbf{x}) - 1 
+ \lambda
+ \mathbf{l} ^ \intercal \mathbf{x} 
+ \mathbf{m} ^ \intercal (\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{m}.
\end{equation}
%
Setting the left hand side to zero gives
%
\begin{equation}
p(\mathbf{x}) 
= \exp \left( 
- 1 
+ \lambda
+ \mathbf{l} ^ \intercal \mathbf{x} 
+ \mathbf{m} ^ \intercal (\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{m} 
\right),
\end{equation}
%
so that
%
\begin{equation}
p(\mathbf{x}) 
= \exp \left( 
- 1 
+ \lambda
- \mathbf{l} ^ \intercal \mathbf{M} \mathbf{l}
+ \left( \mathbf{x} - \bm{\mu} - \mathbf{M} \mathbf{l} \right) ^ \intercal \mathbf{M} ^ {- 1} \left( \mathbf{x} - \bm{\mu} - \mathbf{M} \mathbf{l} \right)
\right),
\end{equation}
%
where
%
\begin{equation}
\mathbf{M} = \left( \mathbf{m} \mathbf{m} ^ \intercal \right) ^ {- 1}.
\end{equation}
%
Substituting it to 
%
\begin{equation}
\begin{aligned}
\int p(\mathbf{x}) d\mathbf{x} &= 1, \\
\int \mathbf{x} p(\mathbf{x}) d\mathbf{x} &= \bm{\mu}, \\
\int (\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal p(\mathbf{x}) d\mathbf{x} &= \bm{\Sigma},
\end{aligned}
\end{equation}
%
and the transformation 
%
\begin{equation}
\mathbf{y} = \mathbf{x} - \bm{\mu} - \mathbf{M} \mathbf{l}
\end{equation}
%
gives
%
\begin{equation}
\begin{aligned}
\exp \left( 
- 1 
+ \lambda
- \mathbf{l} ^ \intercal \mathbf{M} \mathbf{l} 
\right) 
\int 
\exp \left( 
- \mathbf{y} ^ \intercal \mathbf{M} ^ {- 1} \mathbf{y} 
\right) 
d\mathbf{y} 
&= 1, \\
\exp \left( 
- 1 
+ \lambda
- \mathbf{l} ^ \intercal \mathbf{M} \mathbf{l} 
\right) 
\int 
\left( 
\mathbf{y} 
+ \bm{\mu} 
+ \mathbf{M} \mathbf{l} \right) 
\exp \left( - \mathbf{y} ^ \intercal \mathbf{M} ^ {- 1} \mathbf{y} \right) 
d\mathbf{y} 
&= \bm{\mu}, \\
\exp \left( 
- 1 
+ \lambda
- \mathbf{l} ^ \intercal \mathbf{M} \mathbf{l} 
\right) 
\int 
\left( \mathbf{y} 
+ \mathbf{M} \mathbf{l} 
\right) 
\left( \mathbf{y} 
+ \mathbf{M} \mathbf{l} \right) ^ \intercal
\exp \left( - \mathbf{y} ^ \intercal \mathbf{M} ^ {- 1}\mathbf{y} \right) 
d\mathbf{y} 
&= \bm{\Sigma}.
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
\begin{aligned}
\int \exp \left( - \mathbf{y} ^ \intercal \mathbf{y} \right) d\mathbf{y} 
&= \left( \Gamma \left( \frac{1}{2} \right) \right) ^ D, \\
\int \mathbf{y} \exp \left( - \mathbf{y} ^ \intercal \mathbf{y} \right) d\mathbf{y} 
&= \mathbf{0}, \\
\int \mathbf{y} \mathbf{y} ^ \intercal \exp \left( - \mathbf{y} ^ \intercal \mathbf{y} \right) d\mathbf{y}
&= \Gamma \left( \frac{3}{2} \right) \left( \Gamma \left( \frac{1}{2} \right) \right) ^ {D - 1} \mathbf{I}, \\
\end{aligned}
\end{equation}
%
they can  be written as
%
\begin{equation}
\begin{aligned}
\exp \left( 
- 1 
+ \lambda
- \mathbf{l} ^ \intercal \mathbf{M} \mathbf{l} 
\right) 
\left( \Gamma \left( \frac{1}{2} \right) \right) ^ D 
(\det \mathbf{M}) ^ \frac{1}{2}
&= 1, \\
\exp \left( 
- 1 
+ \lambda
- \mathbf{l} ^ \intercal \mathbf{M} \mathbf{l} 
\right)
(\bm{\mu} + \mathbf{M} \mathbf{l}) 
\left( \Gamma \left( \frac{1}{2} \right) \right) ^ D 
(\det \mathbf{M}) ^ \frac{1}{2}
&= \bm{\mu}, \\
\exp \left( 
- 1 
+ \lambda
- \mathbf{l} ^ \intercal \mathbf{M} \mathbf{l} 
\right)
\left( \Gamma \left( \frac{3}{2} \right) \left( \Gamma \left( \frac{1}{2} \right) \right) ^ {D - 1} \mathbf{M} 
+ \mathbf{M} \mathbf{l} (\mathbf{M} \mathbf{l}) ^ \intercal 
\left( \Gamma \left( \frac{1}{2} \right) \right) ^ D 
\right) 
(\det \mathbf{M}) ^ \frac{1}{2}
&= \bm{\Sigma}. 
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
\begin{aligned}
\lambda &= 1 - \frac{D}{2} \ln \pi - \frac{1}{2} \ln (\det \mathbf{M}), \\
\mathbf{l} &= \mathbf{0}, \\
\mathbf{M} &= 2 \bm{\Sigma}.
\end{aligned}
\end{equation}
%
Thus,
%
\begin{equation}
p(\mathbf{x}) = (2 \pi) ^ {- \frac{D}{2}} (\det \bm{\Sigma}) ^ {- \frac{1}{2}} \exp \left( - \frac{1}{2} (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) \right).
\end{equation}
%


\subsection{}
\label{2.15}
Let $\mathbf{x}$ be a variable in $D$ dimensions such that
%
\begin{equation}
p(\mathbf{x}) = \mathcal{N} (\mathbf{x} | \bm{\mu}, \bm{\Sigma}).
\end{equation}
%
Then, by the definition,
%
\begin{equation}
{\rm H}(\mathbf{x}) = - \int \mathcal{N} \left( \mathbf{x} | \mathbf{\mu}, \mathbf{\Sigma} \right) \ln \mathcal{N} \left( \mathbf{x} | \mathbf{\mu}, \mathbf{\Sigma} \right) d\mathbf{x}.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\begin{aligned}
&- \int \mathcal{N} \left( \mathbf{x} | \bm{\mu}, \bm{\Sigma} \right) \left( - \frac{D}{2} \ln (2 \pi) - \frac{1}{2} \ln \abs{\det \bm{\Sigma}} - \frac{1}{2} (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) \right) d\mathbf{x} \\
=& \left( \frac{D}{2} \ln (2 \pi) + \frac{1}{2} \ln \abs{\det \bm{\Sigma}} \right) \int \mathcal{N} \left( \mathbf{x} | \bm{\mu}, \bm{\Sigma} \right) d\mathbf{x} \\
&+ \frac{1}{2} \int (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) \mathcal{N} \left( \mathbf{x} | \bm{\mu}, \bm{\Sigma} \right) d\mathbf{x}.
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
\begin{aligned}
\int \mathcal{N} \left( \mathbf{x} | \bm{\mu}, \bm{\Sigma} \right) d\mathbf{x} &= 1, \\
\int (\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal \mathcal{N} \left( \mathbf{x} | \bm{\mu}, \bm{\Sigma} \right) d\mathbf{x} &= \bm{\Sigma},
\end{aligned}
\end{equation}
%
the first and second term of the right hand side can be written as
%
\begin{equation}
\frac{D}{2} \ln (2 \pi) + \frac{1}{2} \ln \abs{\det \bm{\Sigma}}
\end{equation}
%
and
%
\begin{equation}
\frac{1}{2} \tr \left( \bm{\Sigma} ^ {- 1} \bm{\Sigma} \right) = \frac{D}{2}.
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm H}(\mathbf{x}) = \frac{D}{2} \left( 1 + \ln (2 \pi) \right) + \frac{1}{2} \ln \abs{\det \bm{\Sigma}}.
\end{equation}
%


\subsection{}
\label{2.16}
Let $x$ be a variable such that
\begin{equation}
x = x_1 + x_2,
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
p(x_1) &= \mathcal{N} \left(x_1 | \mu_1, \tau_1 ^ {- 1} \right), \\
p(x_2) &= \mathcal{N} \left(x_2 | \mu_2, \tau_2 ^ {- 1} \right).
\end{aligned} 
\end{equation}
%
Then
%
\begin{equation}
p(x) = \int_{- \infty}^{\infty} p(x | x_2) p(x_2) dx_2.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\begin{aligned}
&\int_{- \infty}^{\infty} \mathcal{N} (x | \mu_1 + x_2, \tau_1 ^ {-1}) \mathcal{N} \left(x_2 | \mu_2, \tau_2 ^ {- 1} \right) dx_2 \\
&= \int_{- \infty}^{\infty} \left( \frac{\tau_1}{2 \pi} \right) ^ \frac{1}{2} \exp \left( - \frac{\tau_1}{2} (x - \mu_1 - x_2) ^ 2 \right) \left( \frac{\tau_2}{2 \pi} \right) ^ \frac{1}{2} \exp \left( - \frac{\tau_2}{2} (x_2 - \mu_2) ^ 2 \right) dx_2.
\end{aligned}
\end{equation}
%
The logarithm of the integrand except the terms independent of $x$ and $z$ is given by
%
\begin{equation}
\begin{aligned}
& 
- \frac{\tau_1 + \tau_2}{2} 
\left( 
	x_2 
	- \frac{\tau_1 (x - \mu_1) + \tau_2 \mu_2}{\tau_1 + \tau_2} 
\right) ^ 2
- \frac{\tau_1}{2} (x - \mu_1) ^ 2 
- \frac{\tau_2}{2} \mu_2 ^ 2 
+ \frac{\tau_1 + \tau_2}{2} 
\left( 
	\frac{\tau_1 (x - \mu_1) + \tau_2 \mu_2}{\tau_1 + \tau_2} 
\right) ^ 2
\\
&=
- \frac{\tau_1 + \tau_2}{2} 
\left( 
	x_2 
	- \frac{\tau_1 (x - \mu_1) + \tau_2 \mu_2}{\tau_1 + \tau_2} 
\right) ^ 2
- \frac{\tau_1 \tau_2}{2 (\tau_1 + \tau_2)} 
( x - \mu_1 - \mu_2 ) ^ 2
.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
p(x) = \mathcal{N} \left( x \left| \mu_1 + \mu_2, \tau_1 ^ {- 1} + \tau_2 ^ {-1} \right. \right).
\end{equation}
%
Thus, by \ref{1.35},
%
\begin{equation}
{\rm H}(x) = \frac{1}{2} \left( 1 + \ln (2 \pi) + \ln \left( \tau_1 ^ {- 1} + \tau_2 ^ {-1} \right) \right).
\end{equation}
%


\subsection{}
\label{2.17}
Let $\bm{\Sigma}$ be a matrix and
%
\begin{equation}
\begin{aligned}
\mathbf{S} &= \frac{1}{2} \left( \bm{\Sigma} ^ {- 1} + \left( \bm{\Sigma} ^ {- 1} \right) ^ \intercal \right), \\
\mathbf{A} &= \frac{1}{2} \left( \bm{\Sigma} ^ {- 1} - \left( \bm{\Sigma} ^ {- 1} \right) ^ \intercal \right).
\end{aligned}
\end{equation}
%
Then
%
\begin{equation}
\bm{\Sigma} ^ {- 1} = \mathbf{S} + \mathbf{A}.
\end{equation}
%
Therefore,
%
\begin{equation}
(\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {-1} (\mathbf{x} - \bm{\mu})
= (\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{S} (\mathbf{x} - \bm{\mu}) 
+ (\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{A} (\mathbf{x} - \bm{\mu}) .
\end{equation}
%
The second term of the right hand side can be written as
%
\begin{equation}
\frac{1}{2} (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) - \frac{1}{2} (\mathbf{x} - \bm{\mu}) ^ \intercal \left( \bm{\Sigma} ^ {- 1} \right) ^ \intercal (\mathbf{x} - \bm{\mu}).
\end{equation}
%
The second term of the right hand side can be written as
%
\begin{equation}
- \frac{1}{2} \left( \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) \right) ^ \intercal (\mathbf{x} - \bm{\mu})
= - \frac{1}{2} (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}).
\end{equation}
%
Thus,
%
\begin{equation}
(\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{A} (\mathbf{x} - \bm{\mu}) 
= 0.
\end{equation}
%
Hence
%
\begin{equation}
(\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu})
= (\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{S} (\mathbf{x} - \bm{\mu}).
\end{equation}
%


\subsection{}
\label{2.18}
Let $\bm{\Sigma}$ be a $D \times D$ real symmetric matrix such that
%
\begin{equation}
\bm{\Sigma} \mathbf{u}_i = \lambda_i \mathbf{u}_i,
\end{equation}
%
where $i = 1, \cdots, D$ and $\mathbf{u}_i$ are unit vectors.
Taking the inner product with $\overline{\mathbf{u}_i}$ on both sides gives
%
\begin{equation}
\overline{\mathbf{u}_i} ^ \intercal \bm{\Sigma} \mathbf{u}_i
= \lambda_i.
\end{equation}
%
Since $\bm{\Sigma}$ is real and symmetric, the left hand side can be written as
%
\begin{equation}
\overline{\mathbf{u}_i} ^ \intercal \overline{\bm{\Sigma}} ^ \intercal \mathbf{u}_i
= \left( \overline{\bm{\Sigma}} \overline{\mathbf{u}}_i \right) ^ \intercal \mathbf{u}_i.
\end{equation}
%
The right hand side can be writtet as
%
\begin{equation}
\overline{\lambda}_i \overline{\mathbf{u}}_i ^ \intercal \mathbf{u}_i
= \overline{\lambda}_i.
\end{equation}
%
Therefore,
%
\begin{equation}
\lambda_i = \overline{\lambda}_i.
\end{equation}
%

Additionally, for $i \neq j$, taking the inner product with $\mathbf{u}_j$ on n both sides of the original equation gives
%
\begin{equation}
\mathbf{u}_j ^ \intercal \bm{\Sigma} \mathbf{u}_i
= \lambda_i \mathbf{u}_j ^ \intercal \mathbf{u}_i.
\end{equation}
%
Since $\bm{\Sigma}$ is symmetric, the left hand side can be written as
%
\begin{equation}
\mathbf{u}_j ^ \intercal \bm{\Sigma} ^ \intercal \mathbf{u}_i
= \left( \bm{\Sigma} \mathbf{u}_j \right) ^ \intercal \mathbf{u}_i.
\end{equation}
%
The right hand side can be written as $\lambda_j \mathbf{u}_j ^ \intercal \mathbf{u}_i$.
Therefore,
%
\begin{equation}
\lambda_i \mathbf{u}_j ^ \intercal \mathbf{u}_i
= \lambda_j \mathbf{u}_j ^ \intercal \mathbf{u}_i.
\end{equation}
%
Thus, if $\lambda_i \neq \lambda_j$, then
%
\begin{equation}
\mathbf{u}_j ^ \intercal \mathbf{u}_i 
= 0.
\end{equation}
%


\subsection{}
\label{2.19}
Let $\bm{\Sigma}$ be a $D \times D$ real symmetric matrix such that
%
\begin{equation}
\bm{\Sigma} \mathbf{u}_i = \lambda_i \mathbf{u}_i,
\end{equation}
%
where $i = 1, \cdots, D$ and $\mathbf{u}_i$ are unit vectors.
Let 
%
\begin{equation}
\begin{aligned}
\bm{\Lambda}
&= \diag (\lambda_1, \cdots, \lambda_D), \\
\mathbf{U} 
&= [\mathbf{u}_1 \cdots \mathbf{u}_D].
\end{aligned}
\end{equation}
%
Then
%
\begin{equation}
\bm{\Sigma} \mathbf{U}
= \mathbf{U} \bm{\Lambda}.
\end{equation}
%
By \ref{2.18}, 
%
\begin{equation}
\mathbf{U} ^ \intercal \mathbf{U} 
= \mathbf{I}.
\end{equation}
%
Therefore,
%
\begin{equation}
\begin{aligned}
\bm{\Sigma}
&= \mathbf{U} \bm{\Lambda} \mathbf{U} ^ \intercal, \\
\bm{\Sigma} ^ {- 1}
&= \mathbf{U} \bm{\Lambda} ^ {- 1} \mathbf{U} ^ \intercal,
\end{aligned}
\end{equation}
%
Thus,
%
\begin{equation}
\begin{aligned}
\bm{\Sigma}
&= \sum_{i = 1}^{D} \lambda_i \mathbf{u}_i \mathbf{u}_i ^ \intercal, \\
\bm{\Sigma} ^ {- 1}
&= \sum_{i = 1}^{D} \frac{1}{\lambda_i} \mathbf{u}_i \mathbf{u}_i ^ \intercal.
\end{aligned}
\end{equation}
%


\subsection{}
\label{2.20}
Let $\bm{\Sigma}$ be a $D \times D$ real symmetric matrix such that
%
\begin{equation}
\bm{\Sigma} \mathbf{u}_i = \lambda_i \mathbf{u}_i,
\end{equation}
%
where $i = 1, \cdots, D$ and $\mathbf{u}_i$ are unit vectors.
Let 
%
\begin{equation}
\begin{aligned}
\bm{\Lambda}
&= \diag (\lambda_1, \cdots, \lambda_D), \\
\mathbf{U} 
&= [\mathbf{u}_1 \cdots \mathbf{u}_D].
\end{aligned}
\end{equation}
%
By \ref{2.19}, 
%
\begin{equation}
\mathbf{a} ^ \intercal \bm{\Sigma} \mathbf{a}
= \mathbf{b} ^ \intercal \bm{\Lambda} \mathbf{b},
\end{equation}
%
where
%
\begin{equation}
\mathbf{b}
= \mathbf{U} ^ \intercal \mathbf{a}.
\end{equation}
%
The right hand side can be written as $\sum_{i = 1}^{D} \lambda_i b_i ^ 2$.
Therefore, the necessary and sufficient condition for
%
\begin{equation}
\mathbf{a} ^ \intercal \bm{\Sigma} \mathbf{a}
> 0
\end{equation}
%
for any real vector $\mathbf{a}$ is 
%
\begin{equation}
\lambda_i > 0.
\end{equation}
%


\subsection{}
\label{2.21}
Let $\bm{\Sigma}$ be a $D \times D$ real symmetric matrix.
Then the number of independent parameters is $\frac{D (D + 1)}{2}$.


\subsection{}
\label{2.22}
Let $\bm{\Sigma}$ be a $D \times D$ symmetric matrix and
%
\begin{equation}
\bm{\Sigma} \bm{\Lambda}
= \mathbf{I}.
\end{equation}
%
Taking the transpose of the both sides gives
%
\begin{equation}
\bm{\Lambda} ^ \intercal \bm{\Sigma} = \mathbf{I}.
\end{equation}
%
Therefore,
%
\begin{equation}
\bm{\Lambda} ^ \intercal
= \bm{\Lambda}.
\end{equation}
%


\subsection{}
\label{2.23}
Let $\bm{\Sigma}$ be a $D \times D$ real symmetric matrix such that
%
\begin{equation}
\bm{\Sigma} \mathbf{u}_i = \lambda_i \mathbf{u}_i,
\end{equation}
%
where $i = 1, \cdots, D$ and $\mathbf{u}_i$ are unit vectors.
Let 
%
\begin{equation}
\begin{aligned}
\bm{\Lambda}'
&= \diag \left( \lambda_1 ^ {- \frac{1}{2}}, \cdots, \lambda_D ^ {- \frac{1}{2}} \right), \\
\mathbf{U} 
&= [\mathbf{u}_1 \cdots \mathbf{u}_D].
\end{aligned}
\end{equation}
%
By \ref{2.19}, 
%
\begin{equation}
\int_{(\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) = \Delta} d\mathbf{x} 
= \int_{(\mathbf{x} - \bm{\mu}) ^ \intercal \mathbf{U} \bm{\Lambda}' {\bm{\Lambda}'} ^ \intercal \mathbf{U} ^ \intercal  (\mathbf{x} - \bm{\mu}) = \Delta} d\mathbf{x}.
\end{equation}
%
By the transformation
%
\begin{equation}
\mathbf{y}
= {\bm{\Lambda}'} ^ \intercal \mathbf{U} ^ \intercal  (\mathbf{x} - \bm{\mu})
\end{equation}
%
and the property
%
\begin{equation}
\mathbf{U} ^ \intercal \mathbf{U} = \mathbf{I},
\end{equation}
%
the right hand side can be written as
%
\begin{equation}
\int_{\norm{\mathbf{y}} ^ 2 = \Delta} \abs{\det \left( \mathbf{U} {\mathbf{\Lambda}'} ^ {- 1} \right)} d\mathbf{y}
= \abs{\det \mathbf{\Sigma}} ^ \frac{1}{2} \int_{\norm{\mathbf{y}} ^ 2 = \Delta} d\mathbf{y}.
\end{equation}
%
Therefore,
%
\begin{equation}
\int_{(\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1}  (\mathbf{x} - \bm{\mu}) = \Delta} d\mathbf{x}
= \abs{\det \mathbf{\Sigma}} ^ \frac{1}{2} \Delta ^ D V_D,
\end{equation}
%
where
%
\begin{equation}
V_D
= \int_{\norm{\mathbf{x}} = 1} d\mathbf{x}.
\end{equation}
%


\subsection{}
\label{2.24}
Let
%
\[
\begin{bmatrix}
\mathbf{X} & \mathbf{Y} \\
\mathbf{Z} & \mathbf{W}
\end{bmatrix}
=
\begin{bmatrix}
\mathbf{A} & \mathbf{B} \\
\mathbf{C} & \mathbf{D}
\end{bmatrix}
^ {-1}.
\]
%
Then
%
\begin{equation}
\begin{aligned}
\mathbf{X} \mathbf{A} + \mathbf{Y} \mathbf{C} 
&= \mathbf{I}, \\
\mathbf{X} \mathbf{B} + \mathbf{Y} \mathbf{D} 
&= \mathbf{O}, \\
\mathbf{Z} \mathbf{A} + \mathbf{W} \mathbf{C} 
&= \mathbf{O}, \\
\mathbf{Z} \mathbf{B} + \mathbf{W} \mathbf{D} 
&= \mathbf{I}.
\end{aligned}
\end{equation}
%
By the second and third equations,
%
\begin{equation}
\begin{aligned}
\mathbf{Y} 
&= - \mathbf{X} \mathbf{B} \mathbf{D} ^ {- 1}, \\
\mathbf{W} 
&= - \mathbf{Z} \mathbf{A} \mathbf{C} ^ {- 1}.
\end{aligned}
\end{equation}
%
Substituting them to the first and fourth equation gives
%
\begin{equation}
\begin{aligned}
\mathbf{X} \left( \mathbf{A} - \mathbf{B} \mathbf{D} ^ {- 1} \mathbf{C} \right)
&= \mathbf{I}, \\
\mathbf{Z} \left( \mathbf{B} - \mathbf{A} \mathbf{C} ^ {- 1} \mathbf{D} \right)
&= \mathbf{I}.
\end{aligned}
\end{equation}
%
Therefore,
%
\[
\begin{bmatrix}
\mathbf{X} & \mathbf{Y} \\
\mathbf{Z} & \mathbf{W}
\end{bmatrix}
=
\begin{bmatrix}
\left( \mathbf{A} - \mathbf{B} \mathbf{D} ^ {- 1} \mathbf{C} \right) ^ {- 1} 
& - \left( \mathbf{A} - \mathbf{B} \mathbf{D} ^ {- 1} \mathbf{C} \right) ^ {- 1} \mathbf{B} \mathbf{D} ^ {- 1} \\
\left( \mathbf{B} - \mathbf{A} \mathbf{C} ^ {- 1} \mathbf{D} \right) ^ {- 1} 
& - \left( \mathbf{B} - \mathbf{A} \mathbf{C} ^ {- 1} \mathbf{D} \right) ^ {- 1} \mathbf{A} \mathbf{C} ^ {- 1}
\end{bmatrix}
.
\]
%


\subsection{}
\label{2.25}
Let $\mathbf{x}$ be a variable in $D$ dimensions such that
%
\begin{equation}
p(\mathbf{x})
= \mathcal{N}(\mathbf{x} | \bm{\mu}, \bm{\Sigma}),
\end{equation}
%
where
%
\[
\mathbf{x}
=
\begin{bmatrix}
\mathbf{x}_a \\
\mathbf{x}_b \\
\mathbf{x}_c
\end{bmatrix}
,
\bm{\mu}
=
\begin{bmatrix}
\bm{\mu}_a \\
\bm{\mu}_b \\
\bm{\mu}_c
\end{bmatrix}
,
\bm{\Sigma}
=
\begin{bmatrix}
\bm{\Sigma}_{aa} & \bm{\Sigma}_{ab} & \bm{\Sigma}_{ac} \\
\bm{\Sigma}_{ba} & \bm{\Sigma}_{bb} & \bm{\Sigma}_{bc} \\
\bm{\Sigma}_{ca} & \bm{\Sigma}_{cb} & \bm{\Sigma}_{cc}
\end{bmatrix}
.
\]
%
Let
%
\begin{equation}
\bm{\Lambda}
= \bm{\Sigma} ^ {-1},
\end{equation}
%
where 
\[
\bm{\Lambda}
=
\begin{bmatrix}
\bm{\Lambda}_{aa} & \bm{\Lambda}_{ab} & \bm{\Lambda}_{ac} \\
\bm{\Lambda}_{ba} & \bm{\Lambda}_{bb} & \bm{\Lambda}_{bc} \\
\bm{\Lambda}_{ca} & \bm{\Lambda}_{cb} & \bm{\Lambda}_{cc}
\end{bmatrix}
.
\]
Then
%
\begin{equation}
\begin{aligned}
&- \frac{1}{2} (\mathbf{x} - \bm{\mu}) ^ \intercal \bm{\Sigma} ^ {- 1} (\mathbf{x} - \bm{\mu}) \\
=& - \frac{1}{2} (\mathbf{x}_a - \bm{\mu}_a) ^ \intercal \bm{\Lambda}_{aa} (\mathbf{x}_a - \bm{\mu}_a)
- \frac{1}{2} (\mathbf{x}_a - \bm{\mu}_a) ^ \intercal \bm{\Lambda}_{ab} (\mathbf{x}_b - \bm{\mu}_b)
- \frac{1}{2} (\mathbf{x}_a - \bm{\mu}_a) ^ \intercal \bm{\Lambda}_{ac} (\mathbf{x}_c - \bm{\mu}_c) \\
& - \frac{1}{2} (\mathbf{x}_b - \bm{\mu}_b) ^ \intercal \bm{\Lambda}_{ba} (\mathbf{x}_a - \bm{\mu}_a)
- \frac{1}{2} (\mathbf{x}_b - \bm{\mu}_b) ^ \intercal \bm{\Lambda}_{bb} (\mathbf{x}_b - \bm{\mu}_b)
- \frac{1}{2} (\mathbf{x}_b - \bm{\mu}_b) ^ \intercal \bm{\Lambda}_{bc} (\mathbf{x}_c - \bm{\mu}_c) \\
& - \frac{1}{2} (\mathbf{x}_c - \bm{\mu}_c) ^ \intercal \bm{\Lambda}_{ca} (\mathbf{x}_a - \bm{\mu}_a)
- \frac{1}{2} (\mathbf{x}_c - \bm{\mu}_c) ^ \intercal \bm{\Lambda}_{cb} (\mathbf{x}_b - \bm{\mu}_b)
- \frac{1}{2} (\mathbf{x}_c - \bm{\mu}_c) ^ \intercal \bm{\Lambda}_{cc} (\mathbf{x}_c - \bm{\mu}_c).
\end{aligned} 
\end{equation}
%
Excluding the terms independent of $\mathbf{x}_a$, the right hand side can be written as
%
\begin{equation}
- \frac{1}{2} (\mathbf{x}_a - \bm{\mu}_{a | b, c}) ^ \intercal \bm{\Sigma}_{a | b, c} ^ {- 1} (\mathbf{x}_a - \bm{\mu}_{a | b, c}),
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
\bm{\mu}_{a | b, c}
&= \bm{\mu}_a 
- \bm{\Lambda}_{aa} ^ {- 1} \bm{\Lambda}_{ab} \left( \mathbf{x}_b - \bm{\mu}_b \right) 
- \bm{\Lambda}_{aa} ^ {- 1} \bm{\Lambda}_{ac} \left( \mathbf{x}_c - \bm{\mu}_c \right), \\
\bm{\Sigma}_{a | b, c}
&= \bm{\Lambda}_{aa} ^ {- 1}.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
p(\mathbf{x}_a | \mathbf{x}_b, \mathbf{x}_c)
= \mathcal{N}(\mathbf{x}_a | \bm{\mu}_{a | b, c}, \bm{\Sigma}_{a | b, c}).
\end{equation}
%
Multiplying both sides by $p(\mathbf{x}_c)$ and integrating both sides with respect to $\mathbf{x}_c$ gives
%
\begin{equation}
p(\mathbf{x}_a | \mathbf{x}_b)
= \int \mathcal{N}(\mathbf{x}_a | \bm{\mu}_{a | b, c}, \bm{\Sigma}_{a | b, c}) p(\mathbf{x}_c) d\mathbf{x}_c.
\end{equation}
%
Thus,
%
\begin{equation}
p(\mathbf{x}_a | \mathbf{x}_b)
= \mathcal{N}(\mathbf{x}_a | \bm{\mu}_{a | b}, \bm{\Sigma}_{a | b}),
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
\bm{\mu}_{a | b}
&= \bm{\mu}_a 
- \bm{\Lambda}_{aa} ^ {- 1} \bm{\Lambda}_{ab} \left( \mathbf{x}_b - \bm{\mu}_b \right) 
+ \bm{\Lambda}_{aa} ^ {- 1} \bm{\Lambda}_{ac} \bm{\mu}_c, \\
\bm{\Sigma}_{a | b}
&= \bm{\Lambda}_{aa} ^ {- 1}.
\end{aligned}
\end{equation}
%


\subsection{(Incomplete)}
\label{2.26}
Since
%
\begin{equation}
\mathbf{I}
= (\mathbf{A} + \mathbf{B} \mathbf{C} \mathbf{D}) 
\left( 
	\mathbf{A} ^ {- 1} 
	- 
	\mathbf{A} ^ {- 1} \mathbf{B} \mathbf{C} \mathbf{D} \mathbf{A} ^ {- 1} 
\right) 
+ 
\left( 
	\mathbf{B} \mathbf{C} \mathbf{D} \mathbf{A} ^ {- 1} 
\right) ^ 2,
\end{equation}
%
we have
%
\begin{equation}
(\mathbf{A} + \mathbf{B} \mathbf{C} \mathbf{D}) ^ {- 1}
=
\mathbf{A} ^ {- 1}
-
\mathbf{A} ^ {- 1}
\mathbf{B}
\left(
	\mathbf{C} ^ {- 1}
	+
	\mathbf{D} \mathbf{A} ^ {- 1} \mathbf{B}
\right)
\mathbf{D} \mathbf{A} ^ {- 1}.
\end{equation}
%


\subsection{}
\label{2.27}
Let $\mathbf{x}$ and $\mathbf{z}$ be two variables.
Then
%
\begin{equation}
{\rm E} (\mathbf{x} + \mathbf{z})
= \int \int (\mathbf{x} + \mathbf{z}) p(\mathbf{x}, \mathbf{z}) d\mathbf{x} d\mathbf{z}.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\int \mathbf{x} \left( \int p(\mathbf{x}, \mathbf{z}) d\mathbf{z} \right) d\mathbf{x}
+ \int \mathbf{z} \left( \int p(\mathbf{x}, \mathbf{z}) d\mathbf{x} \right) d\mathbf{z} \\
= \int \mathbf{x} p(\mathbf{x}) d\mathbf{x}
+ \int \mathbf{z} p(\mathbf{z}) d\mathbf{z}.
\end{equation}
%
The right hand side can be written as ${\rm E} \mathbf{x} + {\rm E} \mathbf{z}$.
Therefore,
%
\begin{equation}
{\rm E} (\mathbf{x} + \mathbf{z})
= {\rm E} \mathbf{x} + {\rm E} \mathbf{z}.
\end{equation}
%

Additionally,
%
\begin{equation}
{\rm cov} (\mathbf{x} + \mathbf{z})
= \int \int \left( \mathbf{x} + \mathbf{z} - {\rm E} (\mathbf{x} + \mathbf{z}) \right) \left( \mathbf{x} + \mathbf{z} - {\rm E} (\mathbf{x} + \mathbf{z}) \right) ^ \intercal p(\mathbf{x}, \mathbf{z}) d\mathbf{x} d\mathbf{z}.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\begin{aligned}
&\int \int \left( \mathbf{x} - {\rm E} \mathbf{x} \right) \left( \mathbf{x} - {\rm E} \mathbf{x} \right) ^ \intercal p(\mathbf{x}, \mathbf{z}) d\mathbf{x} d\mathbf{z}
+ \int \int \left( \mathbf{x} - {\rm E} \mathbf{x} \right) \left( \mathbf{z} - {\rm E} \mathbf{z} \right) ^ \intercal p(\mathbf{x}, \mathbf{z}) d\mathbf{x} d\mathbf{z} \\
&+ \int \int \left( \mathbf{z} - {\rm E} \mathbf{z} \right) \left( \mathbf{x} - {\rm E} \mathbf{x} \right) ^ \intercal p(\mathbf{x}, \mathbf{z}) d\mathbf{x} d\mathbf{z}
+ \int \int \left( \mathbf{z} - {\rm E} \mathbf{z} \right) \left( \mathbf{z} - {\rm E} \mathbf{z} \right) ^ \intercal p(\mathbf{x}, \mathbf{z}) d\mathbf{x} d\mathbf{z}.
\end{aligned}
\end{equation}
%
The first and fourth terms can be written as ${\rm cov} \mathbf{x}$ and ${\rm cov} \mathbf{z}$.
%
If $\mathbf{x}$ and $\mathbf{z}$ are independent, the second and third terms can be written as
%
\begin{equation}
\begin{aligned}
\int \left( \mathbf{x} - {\rm E} \mathbf{x} \right) p(\mathbf{x}) d\mathbf{x}
\int \left( \mathbf{z} - {\rm E} \mathbf{z} \right) ^ \intercal p(\mathbf{z})  d\mathbf{z}
&= \mathbf{O}, \\
\int \left( \mathbf{z} - {\rm E} \mathbf{z} \right) p(\mathbf{z}) d\mathbf{z}
\int \left( \mathbf{x} - {\rm E} \mathbf{x} \right) ^ \intercal p(\mathbf{x})  d\mathbf{x}
&= \mathbf{O}.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm cov} (\mathbf{x} + \mathbf{z})
= {\rm cov} \mathbf{x} + {\rm cov} \mathbf{z}.
\end{equation}
%


\subsection{(Incomplete)}
\label{2.28}
Let 
%
\[
\mathbf{z}
= 
\begin{bmatrix}
\mathbf{x} \\
\mathbf{y}
\end{bmatrix}
,
\]
%
where
%
\[
{\rm E} \mathbf{z}
= 
\begin{bmatrix}
\bm{\mu} \\
\mathbf{A} \bm{\mu} + \mathbf{b}
\end{bmatrix}
\]
%
and
%
\[
{\rm cov} \mathbf{z}
= 
\begin{bmatrix}
\bm{\Lambda} ^ {- 1} & \bm{\Lambda} ^ {- 1} \mathbf{A} ^ \intercal \\
\mathbf{A} \bm{\Lambda} ^ {- 1} & \mathbf{L} ^ {- 1} + \mathbf{A} \bm{\Lambda} ^ {- 1} \mathbf{A} ^ \intercal
\end{bmatrix}
.
\]
%
Then
%
\begin{equation}
\begin{aligned}
\int \left( \mathbf{x} - \bm{\mu} \right) \left( \mathbf{x} - \bm{\mu} \right) ^ \intercal p(\mathbf{x}) d\mathbf{x}
&= \bm{\Lambda} ^ {- 1}, \\
\int \int \left( \mathbf{x} - \bm{\mu} \right) \left( \mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b} \right) ^ \intercal p(\mathbf{x}, \mathbf{y}) d\mathbf{x} d\mathbf{y}
&= \bm{\Lambda} ^ {- 1} \mathbf{A} ^ \intercal, \\
\int \int \left( \mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b} \right) \left( \mathbf{x} - \bm{\mu} \right) ^ \intercal p(\mathbf{x}, \mathbf{y}) d\mathbf{x} d\mathbf{y}
&= \mathbf{A} \bm{\Lambda} ^ {- 1}, \\
\int \left( \mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b} \right) \left( \mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b} \right) ^ \intercal p(\mathbf{y}) d\mathbf{y}
&= \mathbf{L} ^ {- 1} + \mathbf{A} \bm{\Lambda} ^ {- 1} \mathbf{A} ^ \intercal.
\end{aligned}
\end{equation}
%


\subsection{}
\label{2.29}
Let
%
\[
\mathbf{R}
=
\begin{bmatrix}
\bm{\Lambda} + \mathbf{A} ^ \intercal \mathbf{L} \mathbf{A} 
& - \mathbf{A} ^ \intercal \mathbf{L} \\
- \mathbf{L} \mathbf{A}
& \mathbf{L}
\end{bmatrix}
.
\]
%
Then, by \ref{2.24}, 
%
\[
\mathbf{R} ^ {- 1}
=
\begin{bmatrix}
\bm{\Lambda} ^ {- 1} 
& \bm{\Lambda} ^ {- 1}  \mathbf{A} ^ \intercal \\
\mathbf{A} \bm{\Lambda} ^ {- 1}
& \mathbf{L} ^ {- 1} + \mathbf{A} \bm{\Lambda} ^ {- 1} \mathbf{A} ^ \intercal
\end{bmatrix}
.
\]
%


\subsection{}
\label{2.30}
Let
%
\[
\mathbf{R} ^ {- 1}
=
\begin{bmatrix}
\bm{\Lambda} ^ {- 1} 
& \bm{\Lambda} ^ {- 1}  \mathbf{A} ^ \intercal \\
\mathbf{A} \bm{\Lambda} ^ {- 1}
& \mathbf{L} ^ {- 1} + \mathbf{A} \bm{\Lambda} ^ {- 1} \mathbf{A} ^ \intercal
\end{bmatrix}
.
\]
%
Then
%
\[
\mathbf{R} ^ {- 1} 
\begin{bmatrix}
\bm{\Lambda} \bm{\mu} - \mathbf{A} ^ \intercal \mathbf{L} \mathbf{b} \\
\mathbf{L} \mathbf{b}
\end{bmatrix}
=
\begin{bmatrix}
\bm{\mu} \\
\mathbf{A} \bm{\mu} + \mathbf{b}
\end{bmatrix}
.
\]
%


\subsection{}
\label{2.31}
Let $\mathbf{y}$ be a variable such that 
%
\begin{equation}
\mathbf{y}
= \mathbf{x} + \mathbf{z},
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
p(\mathbf{x}) 
&= 
\mathcal{N} 
\left( 
	\mathbf{x} 
	| 
	\bm{\mu}_{\mathbf{x}}, 
	\bm{\Sigma}_{\mathbf{x}} 
\right), \\
p(\mathbf{z}) 
&= 
\mathcal{N} 
\left( 
	\mathbf{z} 
	| 
	\bm{\mu}_{\mathbf{z}}, 
	\bm{\Sigma}_{\mathbf{z}} 
\right).
\end{aligned}
\end{equation}
%
By the definition,
%
\begin{equation}
p(\mathbf{y})
= \int p(\mathbf{y} | \mathbf{x}) p(\mathbf{x}) d\mathbf{x}.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\int 
\mathcal{N} 
\left( 
	\mathbf{y} 
	| 
	\mathbf{x} + \bm{\mu}_{\mathbf{z}}, 
	\bm{\Sigma}_{\mathbf{z}} 
\right) 
\mathcal{N} 
\left( 
	\mathbf{x} 
	| 
	\bm{\mu}_{\mathbf{x}}, 
	\bm{\Sigma}_{\mathbf{x}} 
\right) 
	d\mathbf{x}.
\end{equation}
%
The logarithm of the integrand except the terms independent of $\mathbf{x}$ and $\mathbf{y}$ is given by
%
\begin{equation}
- \frac{1}{2} 
\left( 
	\mathbf{y}
	 - \mathbf{x} 
	 - \bm{\mu}_{\mathbf{z}} 
\right) ^ \intercal 
	\bm{\Sigma}_{\mathbf{z}} ^ {- 1} 
	\left( \mathbf{y} - \mathbf{x} - \bm{\mu}_{\mathbf{z}} 
\right) 
- 
\frac{1}{2} 
(\mathbf{x} - \bm{\mu}_{\mathbf{x}}) ^ \intercal 
\bm{\Sigma}_{\mathbf{x}} ^ {- 1} 
(\mathbf{x} - \bm{\mu}_{\mathbf{x}}).
\end{equation}
%
The first and second order terms can be written as
%
\begin{equation}
-
\mathbf{x} ^ \intercal
\left( 
	\bm{\Sigma}_{\mathbf{z}} ^ {- 1}
	\bm{\mu}_{\mathbf{z}}
	-
	\bm{\Sigma}_{\mathbf{x}} ^ {- 1}
	\bm{\mu}_{\mathbf{x}}
\right)
+
\mathbf{y} ^ \intercal
\bm{\Sigma}_{\mathbf{z}} ^ {- 1}
\bm{\mu}_{\mathbf{z}}
=
\mathbf{u} ^ \intercal
\mathbf{v}
\end{equation}
%
and 
%
\begin{equation}
- 
\frac{1}{2}
\mathbf{x} ^ \intercal
\left( 
	\bm{\Sigma}_{\mathbf{x}} ^ {- 1}
	+ \bm{\Sigma}_{\mathbf{z}} ^ {- 1}
\right)
\mathbf{x} 
+ 
\frac{1}{2} 
\mathbf{x} ^ \intercal 
\bm{\Sigma}_{\mathbf{z}} ^ {- 1}
\mathbf{y}
+ 
\frac{1}{2} 
\mathbf{y} ^ \intercal 
\bm{\Sigma}_{\mathbf{z}} ^ {- 1}
\mathbf{x}
- 
\frac{1}{2} 
\mathbf{y} ^ \intercal 
\bm{\Sigma}_{\mathbf{z}} ^ {- 1} 
\mathbf{y}
=
-\frac{1}{2}
\mathbf{u} ^ \intercal
\mathbf{R}
\mathbf{u},
\end{equation}
%
respectively, where
%
\[
\mathbf{u}
= 
\begin{bmatrix}
\mathbf{x} \\
\mathbf{y}
\end{bmatrix}
,
\mathbf{v}
= 
\begin{bmatrix}
\bm{\Sigma}_{\mathbf{x}} ^ {- 1}
\bm{\mu}_{\mathbf{x}}
-
\bm{\Sigma}_{\mathbf{z}} ^ {- 1}
\bm{\mu}_{\mathbf{z}} 
\\
\bm{\Sigma}_{\mathbf{z}} ^ {- 1}
\bm{\mu}_{\mathbf{z}}
\end{bmatrix}
,
\mathbf{R}
= 
\begin{bmatrix}
\bm{\Sigma}_{\mathbf{x}} ^ {- 1} & - \bm{\Sigma}_{\mathbf{z}} ^ {- 1} \\
- \bm{\Sigma}_{\mathbf{z}} ^ {- 1} & \bm{\Sigma}_{\mathbf{z}} ^ {- 1}
\end{bmatrix}
.
\]
%
Therefore, the logarithm of the integrand except the terms independent of $\mathbf{u}$ can be written as
%
\begin{equation}
-\frac{1}{2}
\left( 
	\mathbf{u}
	- 
	\mathbf{R} ^ {- 1}
	\mathbf{v}
\right) ^ \intercal
\mathbf{R}
\left( 
	\mathbf{u}
	- 
	\mathbf{R} ^ {- 1}
	\mathbf{v}
\right),
\end{equation}
%
where
%
\[
\mathbf{R} ^ {- 1}
=
\begin{bmatrix}
\bm{\Sigma}_\mathbf{x} & \bm{\Sigma}_\mathbf{x} \\
\bm{\Sigma}_\mathbf{x} & \bm{\Sigma}_\mathbf{x} + \bm{\Sigma}_\mathbf{z}
\end{bmatrix}
,
\mathbf{R} ^ {- 1}
\mathbf{v}
=
\begin{bmatrix}
\bm{\mu}_\mathbf{x} \\
\bm{\mu}_\mathbf{x} + \bm{\mu}_\mathbf{z}
\end{bmatrix}
.
\]
%
by \ref{2.29} and \ref{2.30}.
Thus,
%
\begin{equation}
p(\mathbf{y})
= 
\mathcal{N} 
(\mathbf{y} 
| 
\bm{\mu}_\mathbf{x} + \bm{\mu}_\mathbf{z},
\bm{\Sigma}_\mathbf{x} + \bm{\Sigma}_\mathbf{z}
).
\end{equation}
%



\subsection{}
\label{2.32}
Let $\mathbf{x}$ and $\mathbf{y}$ be variables such that 
%
\begin{equation}
\begin{aligned}
p(\mathbf{x})
&=
\mathcal{N}
\left( 
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Lambda} ^ {- 1}
\right), 
\\
p(\mathbf{y} | \mathbf{x})
&=
\mathcal{N}
\left( 
	\mathbf{y}
	|
	\mathbf{A} \mathbf{x} + \mathbf{b},
	\mathbf{L} ^ {- 1}
\right).
\end{aligned}
\end{equation}
%
By the definition,
%
\begin{equation}
p(\mathbf{y} | \mathbf{x})
p(\mathbf{x})
=
p(\mathbf{x} | \mathbf{y})
p(\mathbf{y}).
\end{equation}
%
The logarithm of the left hand side except the terms independent of $\mathbf{x}$ and $\mathbf{y}$ is given by
%
\begin{equation}
- 
\frac{1}{2} 
\left( 
	\mathbf{y}
	 - \mathbf{A} \mathbf{x} 
	 - \mathbf{b} 
\right) ^ \intercal 
\mathbf{L} 
\left( 
	\mathbf{y} 
	- \mathbf{A} \mathbf{x} 
	- \mathbf{b} 
\right) 
- 
\frac{1}{2} 
(\mathbf{x} - \bm{\mu}) ^ \intercal 
\bm{\Lambda}
(\mathbf{x} - \bm{\mu}).
\end{equation}
%
Since the first term can be written as
%
\begin{equation}
\begin{aligned}
&
- 
\frac{1}{2} 
\left( 
	\mathbf{y}
	 - \mathbf{A} (\mathbf{x} - \bm{\mu})
	 - \mathbf{A} \bm{\mu}
	 - \mathbf{b} 
\right) ^ \intercal 
\mathbf{L} 
\left( 
	\mathbf{y}
	 - \mathbf{A} (\mathbf{x} - \bm{\mu})
	 - \mathbf{A} \bm{\mu}
	 - \mathbf{b} 
\right) \\
=&
- 
\frac{1}{2}
(\mathbf{x} - \bm{\mu}) ^ \intercal
\mathbf{A} ^ \intercal
\mathbf{L}
\mathbf{A}
(\mathbf{x} - \bm{\mu})
+
(\mathbf{x} - \bm{\mu}) ^ \intercal
\mathbf{A} ^ \intercal
\mathbf{L}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}) \\
&-
\frac{1}{2}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}) ^ \intercal
\mathbf{L}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}),
\end{aligned}
\end{equation}
%
the logarithm except the terms independent of $\mathbf{x}$ and $\mathbf{y}$ can be written as
%
\begin{equation}
\begin{aligned}
&-
\frac{1}{2}
(\mathbf{x} - \bm{\mu} - \mathbf{z}) ^ \intercal
\left(
	\bm{\Lambda}
	+
	\mathbf{A} ^ \intercal
	\mathbf{L}
	\mathbf{A}
\right)
(\mathbf{x} - \bm{\mu} - \mathbf{z})
+
\frac{1}{2}
\mathbf{z} ^ \intercal
\left(
	\mathbf{A} ^ \intercal
	\mathbf{L}
	\mathbf{A}
	+
	\bm{\Lambda}
\right)
\mathbf{z} \\
&-
\frac{1}{2}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}) ^ \intercal
\mathbf{L}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}) \\
=
&-
\frac{1}{2}
(\mathbf{x} - \bm{\mu} - \mathbf{z}) ^ \intercal
\left(
	\bm{\Lambda}
	+
	\mathbf{A} ^ \intercal
	\mathbf{L}
	\mathbf{A}
\right)
(\mathbf{x} - \bm{\mu} - \mathbf{z})
-
\frac{1}{2}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}) ^ \intercal
\mathbf{M}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}),
\end{aligned}
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
\mathbf{z}
&= 
\left( 
	\bm{\Lambda}
	+
	\mathbf{A} ^ \intercal
	\mathbf{L}
	\mathbf{A}
\right) ^ {- 1}
\mathbf{A} ^ \intercal
\mathbf{L}
(\mathbf{y} - \mathbf{A} \bm{\mu} - \mathbf{b}), 
\\
\mathbf{M}
&=
\mathbf{L}
-
\mathbf{L}
\mathbf{A}
\left( 
	\bm{\Lambda}
	+
	\mathbf{A} ^ \intercal
	\mathbf{L}
	\mathbf{A}
\right) ^ {- 1}
\mathbf{A} ^ \intercal
\mathbf{L}.
\end{aligned}
\end{equation}
%
we have
%
\begin{equation}
\bm{\mu}
+
\mathbf{z}
=
\left( 
	\bm{\Lambda}
	+
	\mathbf{A} ^ \intercal
	\mathbf{L}
	\mathbf{A}
\right) ^ {- 1}
\left(
	\mathbf{A} ^ \intercal
	\mathbf{L}
	(\mathbf{y} - \mathbf{b})
	+
	\bm{\Lambda}
	\bm{\mu}
\right).
\end{equation}
%
By \ref{2.26},
%
\begin{equation}
\left( 
	\bm{\Lambda}
	+
	\mathbf{A} ^ \intercal
	\mathbf{L}
	\mathbf{A}
\right) ^ {- 1}
=
\bm{\Lambda} ^ {- 1}
-
\bm{\Lambda} ^ {- 1}
\mathbf{A} ^ \intercal
\left(
	\mathbf{L} ^ {- 1}
	+
	\mathbf{A}
	\bm{\Lambda} ^ {- 1}
	\mathbf{A} ^ \intercal
\right) ^ {- 1}
\mathbf{A}
\bm{\Lambda} ^ {- 1}.
\end{equation}
%
Therefore,
%
\begin{equation}
\mathbf{M}
=
\left(
	\mathbf{L} ^ {- 1}
	+
	\mathbf{A}
	\bm{\Lambda} ^ {- 1}
	\mathbf{A} ^ \intercal
\right) ^ {- 1}.
\end{equation}
%
Thus,
%
\begin{equation}
\begin{aligned}
p(\mathbf{x} | \mathbf{y})
&=
\mathcal{N}
\left(
	\mathbf{x}
	|
	\left( 
		\bm{\Lambda}
		+
		\mathbf{A} ^ \intercal
		\mathbf{L}
		\mathbf{A}
	\right) ^ {- 1}
	\left(
		\mathbf{A} ^ \intercal
		\mathbf{L}
		(\mathbf{y} - \mathbf{b})
		+
		\bm{\Lambda}
		\bm{\mu}
	\right),
	\left( 
		\bm{\Lambda}
		+
		\mathbf{A} ^ \intercal
		\mathbf{L}
		\mathbf{A}
	\right) ^ {- 1}
\right),
\\
p(\mathbf{y})
&=
\mathcal{N}
\left(
	\mathbf{y}
	|
	\mathbf{A}
	\bm{\mu}
	+
	\mathbf{b},
	\mathbf{L} ^ {- 1}
	+
	\mathbf{A}
	\bm{\Lambda} ^ {- 1}
	\mathbf{A} ^ \intercal
\right).
\end{aligned}
\end{equation}
%


\subsection{}
\label{2.33}
Refer to \ref{2.32}.


\subsection{}
\label{2.34}
Let $\mathbf{X}$ be a set of $N$ variables such that
%
\begin{equation}
\ln p
\left(
	\mathbf{X}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
=
-
\frac{N D}{2}
\ln (2 \pi)
-
\frac{N}{2}
\ln (\det \bm{\Sigma})
-
\frac{1}{2}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\mathbf{x}_n - \bm{\mu}).
\end{equation}
%
To maximise it with respect to $\bm{\mu}$ and $\bm{\Sigma}$, setting the partial derivatives to zero gives
%
\begin{equation}
\begin{aligned}
\mathbf{0}
&= 
\sum_{n = 1}^{N}
\left(
	\bm{\Sigma} ^ {- 1}
	+
	\left(
		\bm{\Sigma} ^ {- 1}
	\right) ^ \intercal
\right)
(\mathbf{x}_n - \bm{\mu}),
\\
\mathbf{O}
&=
-
\frac{N}{2}
\left( 
	\bm{\Sigma} ^ {- 1}
\right) ^ \intercal
+
\frac{1}{2}
\left( \bm{\Sigma} ^ {- 1} \right) ^ 2
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu})
(\mathbf{x}_n - \bm{\mu}) ^ \intercal.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
\begin{aligned}
\bm{\mu}_{{\rm ML}}
&= 
\frac{1}{N}
\sum_{n = 1}^{N} 
\mathbf{x}_n,
\\
\bm{\Sigma}_{{\rm ML}}
&=
\frac{1}{N}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}_{\rm ML})
(\mathbf{x}_n - \bm{\mu}_{\rm ML}) ^ \intercal.
\end{aligned}
\end{equation}
%


\subsection{}
\label{2.35}
Let $\mathbf{x}$ be a variable such that
%
\begin{equation}
p(\mathbf{x})
=
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right).
\end{equation}
%
Then
%
\begin{equation}
{\rm E} \mathbf{x} \mathbf{x} ^ \intercal
=
\int 
\mathbf{x} \mathbf{x} ^ \intercal
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x}.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\begin{aligned}
&\int 
(\mathbf{x} - \bm{\mu} + \bm{\mu}) (\mathbf{x} - \bm{\mu} + \bm{\mu}) ^ \intercal
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x} \\
=&
\int 
(\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x}
+
\bm{\mu}
\int 
(\mathbf{x} - \bm{\mu}) ^ \intercal
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x} \\
&+
\left(
	\int
	(\mathbf{x} - \bm{\mu})
	\mathcal{N}
	\left(
		\mathbf{x}
		|
		\bm{\mu},
		\bm{\Sigma}
	\right)
	d\mathbf{x}
\right)
\bm{\mu} ^ \intercal
+
\bm{\mu} \bm{\mu} ^ \intercal
\int 
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x}.
\end{aligned}
\end{equation}
%
Since
%
\begin{equation}
\begin{aligned}
\int 
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x}
&=
1, \\
\int 
\mathbf{x}
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x}
&=
\bm{\mu},
\\
\int 
(\mathbf{x} - \bm{\mu}) (\mathbf{x} - \bm{\mu}) ^ \intercal
\mathcal{N}
\left(
	\mathbf{x}
	|
	\bm{\mu},
	\bm{\Sigma}
\right)
d\mathbf{x}
&=
\bm{\Sigma},
\end{aligned}
\end{equation}
%
the right hand side can be written as $\bm{\Sigma} + \bm{\mu} \bm{\mu} ^ \intercal$.
Therefore,
%
\begin{equation}
{\rm E} \mathbf{x} \mathbf{x} ^ \intercal
=
\bm{\Sigma} + \bm{\mu} \bm{\mu} ^ \intercal.
\end{equation}
%

Additionally, let $\mathbf{x}_n$ and $\mathbf{x}_m$ be variables such that
%
\begin{equation}
\begin{aligned}
p(\mathbf{x}_n)
&=
\mathcal{N}
\left(
	\mathbf{x_n}
	|
	\bm{\mu},
	\bm{\Sigma}
\right), 
\\
p(\mathbf{x}_m)
&=
\mathcal{N}
\left(
	\mathbf{x_m}
	|
	\bm{\mu},
	\bm{\Sigma}
\right).
\end{aligned}
\end{equation}
%
If $n \neq m$, then
%
\begin{equation}
{\rm E} \mathbf{x}_n \mathbf{x}_m ^ \intercal
= 
{\rm E} \mathbf{x}_n {\rm E} \mathbf{x}_m ^ \intercal.
\end{equation}
%
The right hand side can be written as $\bm{\mu} \bm{\mu} ^ \intercal$.
Therefore,
%
\begin{equation}
{\rm E} \mathbf{x}_n \mathbf{x}_m ^ \intercal
=
\delta_{nm} \bm{\Sigma} + \bm{\mu} \bm{\mu} ^ \intercal.
\end{equation}
%

Finally, let $\mathbf{x}_1, \cdots, \mathbf{x}_N$ be variables such that
%
\begin{equation}
p(\mathbf{x}_n)
=
\mathcal{N}
\left(
	\mathbf{x}_n
	|
	\bm{\mu},
	\bm{\Sigma}
\right).
\end{equation}
% 
By \ref{2.34},
%
\begin{equation}
\begin{aligned}
\bm{\mu}_{{\rm ML}}
&= 
\frac{1}{N}
\sum_{n = 1}^{N} 
\mathbf{x}_n,
\\
\bm{\Sigma}_{{\rm ML}}
&=
\frac{1}{N}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}_{\rm ML})
(\mathbf{x}_n - \bm{\mu}_{\rm ML}) ^ \intercal.
\end{aligned}
\end{equation}
%
Then
%
\begin{equation}
{\rm E} \bm{\Sigma}_{\rm ML}
=
\frac{1}{N}
\sum_{n = 1}^{N}
{\rm E}
(\mathbf{x}_n - \bm{\mu}_{\rm ML})
(\mathbf{x}_n - \bm{\mu}_{\rm ML}) ^ \intercal.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\begin{aligned}
&
\frac{1}{N}
\sum_{n = 1}^{N}
{\rm E}
\mathbf{x}_n
\mathbf{x}_n ^ \intercal
-
\frac{1}{N ^ 2}
\sum_{n = 1}^{N}
{\rm E}
\left(
	\sum_{n = 1}^{N}
	\mathbf{x_n}
\right)
\mathbf{x}_n ^ \intercal
-
\frac{1}{N ^ 2}
\sum_{n = 1}^{N}
{\rm E}
\mathbf{x}_n
\left(
	\sum_{n = 1}^{N}
	\mathbf{x_n}
\right) ^ \intercal \\
&+
\frac{1}{N ^ 3}
\sum_{n = 1}^{N}
{\rm E}
\left(
	\sum_{n = 1}^{N}
	\mathbf{x_n}
\right)
\left(
	\sum_{n = 1}^{N}
	\mathbf{x_n}
\right) ^ \intercal.
\end{aligned}
\end{equation}
%
The first term can be written as $\bm{\Sigma} +  \bm{\mu} \bm{\mu} ^ \intercal$.
%
The second and third terms can be written as
%
\begin{equation}
-
\frac{1}{N}
\left(
	\left(
		\bm{\Sigma}
		+
		\bm{\mu} \bm{\mu} ^ \intercal
	\right)
	+
	(N - 1) \bm{\mu} \bm{\mu} ^ \intercal
\right)
=
-
\frac{1}{N}
\bm{\Sigma}
-
\bm{\mu} \bm{\mu} ^ \intercal.
\end{equation}
%
The fourth term can be written as
%
\begin{equation}
\frac{1}{N ^ 2}
\left(
	N 
	\left(
		\bm{\Sigma}
		+
		\bm{\mu} \bm{\mu} ^ \intercal
	\right)
	+
	N (N - 1)
	\bm{\mu} \bm{\mu} ^ \intercal
\right)
=
\frac{1}{N}
\bm{\Sigma}
+
\bm{\mu} \bm{\mu} ^ \intercal.
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm E} \bm{\Sigma}_{\rm ML}
=
\frac{N - 1}{N} \bm{\Sigma}.
\end{equation}
%


\subsection{}
\label{2.36}
Let $x_1, \cdots, x_N$ be variables such that
%
\begin{equation}
p(x_n)
=
\mathcal{N}
\left(
	x_n
	|
	\mu,
	\sigma ^ 2
\right).
\end{equation}
%
Let us assume that $\mu$ is known.
Then, by \ref{2.34},
%
\begin{equation}
{\sigma_{\rm ML} ^ 2} ^ {(N)}
=
\frac{1}{N}
\sum_{n = 1}^{N}
(x_n - \mu) ^ 2.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\frac{1}{N}
( x_N - \mu) ^ 2
+
\frac{1}{N}
\sum_{n = 1}^{N - 1}
(x_n - \mu) ^ 2
=
\frac{1}{N}
( x_N - \mu) ^ 2
+
\frac{N - 1}{N}
{\sigma_{\rm ML} ^ 2} ^ {(N - 1)}.
\end{equation}
%
Therefore,
%
\begin{equation}
{\sigma_{\rm ML} ^ 2} ^ {(N)}
=
{\sigma_{\rm ML} ^ 2} ^ {(N - 1)}
+
\frac{1}{N}
\left(
	(x_N - \mu) ^ 2
	-
	{\sigma_{\rm ML} ^ 2} ^ {(N - 1)}
\right).
\end{equation}
%
Since
%
\begin{equation}
\frac{\partial}{\partial \sigma ^ 2} 
\left(
	- \ln p
	\left(
		x_n 
		| 
		\sigma ^ 2
	\right)
\right)
=
\frac{1}{2 \sigma ^ 2}
-
\frac{1}{2 \left( \sigma ^ 2 \right) ^ 2} (x_n - \mu) ^ 2,
\end{equation}
%
we have
%
\begin{equation}
{\sigma_{\rm ML} ^ 2} ^ {(N)}
=
{\sigma_{\rm ML} ^ 2} ^ {(N - 1)}
-
\frac{{\sigma_{\rm ML} ^ 2} ^ {(N - 1)}}{N}
\frac{\partial}{\partial {\sigma_{\rm ML} ^ 2} ^ {(N - 1)}} 
\left(
	- \ln p
	\left(
		x_N 
		| 
		{\sigma_{\rm ML} ^ 2} ^ {(N - 1)}
	\right)
\right).
\end{equation}
%


\subsection{}
\label{2.37}
Let $\mathbf{x}_1, \cdots, \mathbf{x}_N$ be variables such that
%
\begin{equation}
p(\mathbf{x}_n)
=
\mathcal{N}
\left(
	\mathbf{x}_n
	|
	\bm{\mu},
	\bm{\Sigma}
\right).
\end{equation}
%
Let us assume that $\bm{\mu}$ is known.
Then, by \ref{2.34},
%
\begin{equation}
\bm{\Sigma}_{\rm ML} ^ {(N)}
=
\frac{1}{N}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu})
(\mathbf{x}_n - \bm{\mu}) ^ \intercal.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\begin{aligned}
&\frac{1}{N}
(\mathbf{x}_N - \bm{\mu})
(\mathbf{x}_N - \bm{\mu}) ^ \intercal
+
\frac{1}{N}
\sum_{n = 1}^{N - 1}
(\mathbf{x}_n - \bm{\mu})
(\mathbf{x}_n - \bm{\mu}) ^ \intercal \\
&=
\frac{1}{N}
(\mathbf{x}_N - \bm{\mu})
(\mathbf{x}_N - \bm{\mu}) ^ \intercal
+
\frac{N - 1}{N}
\bm{\Sigma}_{\rm ML} ^ {(N - 1)}.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
\bm{\Sigma}_{\rm ML} ^ {(N)}
=
\bm{\Sigma}_{\rm ML} ^ {(N - 1)}
+
\frac{1}{N}
\left(
	(\mathbf{x}_N - \bm{\mu})
	(\mathbf{x}_N - \bm{\mu}) ^ \intercal
	-
	\bm{\Sigma}_{\rm ML} ^ {(N - 1)}
\right).
\end{equation}
%
Since
%
\begin{equation}
\frac{\partial}{\partial \bm{\Sigma}} 
\left(
	- \ln p (x_n | \bm{\Sigma})
\right)
=
-
\frac{1}{2}
\left(
	\bm{\Sigma} ^ {- 1}
\right) ^ \intercal
+
\frac{1}{2}
\left(
	\bm{\Sigma} ^ {- 1}
\right) ^ 2
(\mathbf{x}_N - \bm{\mu})
(\mathbf{x}_N - \bm{\mu}) ^ \intercal,
\end{equation}
%
we have
%
\begin{equation}
\bm{\Sigma}_{\rm ML} ^ {(N)}
=
\bm{\Sigma}_{\rm ML} ^ {(N - 1)}
-
\frac{\bm{\Sigma}_{\rm ML} ^ {(N - 1)}}{N}
\frac{\partial}{\partial \bm{\Sigma}_{\rm ML} ^ {(N - 1)}} 
\left(
	- \ln p
	\left(
		\mathbf{x}_N 
		| 
		\bm{\Sigma}_{\rm ML} ^ {(N - 1)}
	\right)
\right).
\end{equation}
%


\subsection{}
\label{2.38}
Let $x_1, \cdots, x_N$ be variables such that
%
\begin{equation}
\begin{aligned}
p(x_n | \mu)
&=
\mathcal{N}
\left(
	x_n
	|
	\mu,
	\sigma ^ 2	
\right),
\\
p(\mu)
&=
\mathcal{N}
\left(
	\mu
	|
	\mu_0,
	\sigma_0 ^ 2	
\right).
\end{aligned}
\end{equation}
%
By the definition,
%
\begin{equation}
p(\mu | \mathbf{x})
p(\mathbf{x})
=
p(\mathbf{x} | \mu)
p(\mu).
\end{equation}
%
The logarithm of the right hand side excpt the terms independent of $\mathbf{x}$ and $\mu$ can be written as
%
\begin{equation}
-
\frac{1}{2 \sigma ^ 2}
\sum_{n = 1}^{N}
(x_n - \mu) ^ 2
-
\frac{1}{2 \sigma_0 ^ 2}
(\mu - \mu_0) ^ 2.
\end{equation}
%
The first term can be written as
%
\begin{equation}
-
\frac{1}{2 \sigma ^ 2}
\sum_{n = 1}^{N}
(x_n - \mu_{\rm ML} + \mu_{\rm ML} - \mu) ^ 2
=
-
\frac{1}{2 \sigma ^ 2}
\sum_{n = 1}^{N}
(x_n - \mu_{\rm ML}) ^ 2
-
\frac{N}{2 \sigma ^ 2}
(\mu_{\rm ML} - \mu) ^ 2.
\end{equation}
%
where
%
\begin{equation}
\mu_{\rm ML}
=
\frac{1}{N}
\sum_{n = 1}^{N}
x_n,
\end{equation}
%
as derived in \ref{2.34}.
Therefore, the logarithm except the terms independent of $\mathbf{x}$ and $\mu$ can be written as
%
\begin{equation}
\begin{aligned}
&-
\frac{1}{2 \sigma ^ 2}
\sum_{n = 1}^{N}
(x_n - \mu_{\rm ML}) ^ 2
-
\frac{N}{2 \sigma ^ 2}
(\mu_{\rm ML} - \mu) ^ 2
-
\frac{1}{2 \sigma_0 ^ 2}
(\mu - \mu_0) ^ 2 \\
&=
-
\frac{1}{2 \sigma ^ 2}
\sum_{n = 1}^{N}
(x_n - \mu_{\rm ML}) ^ 2
-
\frac{1}{2 \sigma_N ^ 2}
\left(
	\mu
	-
	\mu_N	
\right) ^ 2
+
\frac{\mu_N ^ 2}{2 \sigma_N ^ 2},
\end{aligned}
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
\mu_N
&= 
\frac{N \sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} \mu_{\rm ML} 
+
\frac{\sigma ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} \mu_0,
\\
\sigma_N ^ 2
&= 
\frac{\sigma ^ 2 \sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2}.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
p(\mu | \mathbf{x})
= 
\mathcal{N}
\left( 
	\mu
	\left.
	\right \vert
	\frac{N \sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} \mu_{\rm ML} 
	+
	\frac{\sigma ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} \mu_0,
	\frac{\sigma ^ 2 \sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2}
\right).
\end{equation}
%


\subsection{(Incomplete)}
\label{2.39}
Let $x_1, \cdots, x_N$ be variables such that
%
\begin{equation}
\begin{aligned}
p(x_n | \mu)
&=
\mathcal{N}
\left(
	x_n
	|
	\mu,
	\sigma ^ 2	
\right),
\\
p(\mu)
&=
\mathcal{N}
\left(
	\mu
	|
	\mu_0,
	\sigma_0 ^ 2	
\right).
\end{aligned}
\end{equation}
%
Then, by \ref{2.38},
%
\begin{equation}
p(\mu | \mathbf{x})
= 
\mathcal{N}
\left( 
	\mu
	|
	\mu_N,
	\sigma_N ^ 2
\right),
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
\mu_N
&= 
\frac{\sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} 
\sum_{n = 1}^{N}
x_n
+
\frac{\sigma ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} 
\mu_0,
\\
\sigma_N ^ 2
&= 
\frac{\sigma ^ 2 \sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2}.
\end{aligned}
\end{equation}
%
Then
%
\begin{equation}
\begin{aligned}
\mu_N
&= 
\frac{(N - 1) \sigma_0 ^ 2 + \sigma ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2}
\mu_{N - 1}
+
\frac{\sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2}
x_{N}
+
\frac{\sigma ^ 2 - \sigma_0 ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} 
\mu_0,
\\
\sigma_N ^ 2
&= 
\frac{(N - 1) \sigma_0 ^ 2 + \sigma ^ 2}{N \sigma_0 ^ 2 + \sigma ^ 2} 
\sigma_{N - 1} ^ 2.
\end{aligned}
\end{equation}
%


\subsection{}
\label{2.40}
Let $\mathbf{x}_1, \cdots, \mathbf{x}_N$ be variables such that
%
\begin{equation}
\begin{aligned}
p(\mathbf{x}_n | \bm{\mu})
&=
\mathcal{N}
\left(
	\mathbf{x}_n
	|
	\bm{\mu},
	\bm{\Sigma}	
\right),
\\
p(\bm{\mu})
&=
\mathcal{N}
\left(
	\bm{\mu}
	|
	\bm{\mu}_0,
	\bm{\Sigma}	
\right).
\end{aligned}
\end{equation}
%
By the definition,
%
\begin{equation}
p(\bm{\mu} | \mathbf{X})
p(\mathbf{X})
=
p(\mathbf{X} | \bm{\mu})
p(\bm{\mu}).
\end{equation}
%
The logarithm of the right hand side excpt the terms independent of $\mathbf{X}$ and $\bm{\mu}$ can be written as
%
\begin{equation}
-
\frac{1}{2}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\mathbf{x}_n - \bm{\mu})
-
\frac{1}{2}
(\bm{\mu} - \bm{\mu}_0)
\bm{\Sigma}_0 ^ {- 1}
(\bm{\mu} - \bm{\mu}_0) ^ \intercal.
\end{equation}
%
The first term can be written as
%
\begin{equation}
\begin{aligned}
&-
\frac{1}{2}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}_{\rm ML} + \bm{\mu}_{\rm ML} - \bm{\mu}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\mathbf{x}_n - \bm{\mu}_{\rm ML} + \bm{\mu}_{\rm ML} - \bm{\mu}) \\
&=
-
\frac{1}{2}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}_{\rm ML}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\mathbf{x}_n - \bm{\mu}_{\rm ML})
-
\frac{N}{2}
(\bm{\mu}_{\rm ML} - \bm{\mu}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\bm{\mu}_{\rm ML} - \bm{\mu}).
\end{aligned}
\end{equation}
%
where
%
\begin{equation}
\bm{\mu}_{\rm ML}
=
\frac{1}{N}
\sum_{n = 1}^{N}
\mathbf{x}_n,
\end{equation}
%
as derived in \ref{2.34}.
Therefore, the logarithm except the terms independent of $\mathbf{X}$ and $\bm{\mu}$ can be written as
%
\begin{equation}
\begin{aligned}
&-
\frac{1}{2}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}_{\rm ML}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\mathbf{x}_n - \bm{\mu}_{\rm ML})
-
\frac{N}{2}
(\bm{\mu}_{\rm ML} - \bm{\mu}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\bm{\mu}_{\rm ML} - \bm{\mu}) \\
&-
\frac{1}{2}
(\bm{\mu} - \bm{\mu}_0)
\bm{\Sigma}_0 ^ {- 1}
(\bm{\mu} - \bm{\mu}_0) ^ \intercal \\
=&
-
\frac{1}{2}
\sum_{n = 1}^{N}
(\mathbf{x}_n - \bm{\mu}_{\rm ML}) ^ \intercal
\bm{\Sigma} ^ {- 1}
(\mathbf{x}_n - \bm{\mu}_{\rm ML})
-
\frac{1}{2} 
(\bm{\mu} - \bm{\mu}_N) ^ \intercal
\bm{\Sigma}_N ^ {- 1}
(\bm{\mu} - \bm{\mu}_N) \\
&+
\frac{1}{2} 
\bm{\mu}_N ^ \intercal
\bm{\Sigma}_N ^ {- 1}
\bm{\mu}_N,
\end{aligned}
\end{equation}
%
where
%
\begin{equation}
\begin{aligned}
\bm{\mu}_N
&= 
\left(
	N \bm{\Sigma}_0 ^ {- 1}
	+
	\bm{\Sigma} ^ {- 1}
\right) ^ {- 1} 
\left(
	N \bm{\Sigma}_0 ^ {- 1}
	\bm{\mu}_{\rm ML}
	+
	\bm{\Sigma} ^ {- 1}
	\bm{\mu}_0	
\right),
\\
\bm{\Sigma}_N
&= 
\left(
	N \bm{\Sigma}_0 ^ {- 1}
	+
	\bm{\Sigma} ^ {- 1}
\right) ^ {- 1}.
\end{aligned}
\end{equation}
%
Therefore,
%
\begin{equation}
p(\bm{\mu} | \mathbf{X})
= 
\mathcal{N}
\left( 
	\bm{\mu}
	\left.
	\right \vert
	\left(
		N \bm{\Sigma}_0 ^ {- 1}
		+
		\bm{\Sigma} ^ {- 1}
	\right) ^ {- 1} 
	\left(
		N \bm{\Sigma}_0 ^ {- 1}
		\bm{\mu}_{\rm ML}
		+
		\bm{\Sigma} ^ {- 1}
		\bm{\mu}_0	
	\right),
	\left(
		N \bm{\Sigma}_0 ^ {- 1}
		+
		\bm{\Sigma} ^ {- 1}
	\right) ^ {- 1}
\right).
\end{equation}
%


\subsection{}
\label{2.41}
By the definition,
%
\begin{equation}
{\rm Gam} (\lambda | a, b)
=
\frac{1}{\Gamma(a)} 
b ^ a
\lambda ^ {a - 1}
\exp (- b \lambda).
\end{equation}
%
Then
%
\begin{equation}
\int_{0}^{\infty} 
{\rm Gam} (\lambda | a, b)
d\lambda
=
\frac{1}{\Gamma(a)} 
b ^ a
\int_{0}^{\infty}
\lambda ^ {a - 1}
\exp (- b \lambda)
d\lambda.
\end{equation}
%
By the transformation
%
\begin{equation}
\lambda'
= 
b \lambda,
\end{equation}
%
the right hand side can be written as
%
\begin{equation}
\frac{1}{\Gamma(a)} 
b ^ a
\int_{0}^{\infty}
\left(
	\frac{\lambda'}{b}
\right) ^ {a - 1}
\exp (- \lambda')
\frac{1}{b}
d\lambda'
=
\frac{1}{\Gamma(a)}
\int_{0}^{\infty}
{\lambda'} ^ {a - 1}
\exp (- \lambda')
d\lambda'.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\frac{1}{\Gamma(a)}
\Gamma(a)
=
1.
\end{equation}
%
Therefore,
%
\begin{equation}
\int_{0}^{\infty} 
{\rm Gam} (\lambda | a, b)
d\lambda
=
1.
\end{equation}
%


\subsection{}
\label{2.42}
Let $\lambda$ be a variable such that
%
\begin{equation}
p(\lambda)
=
{\rm Gam} (\lambda | a, b).
\end{equation}
%
By the definition,
%
\begin{equation}
{\rm Gam} (\lambda | a, b)
=
\frac{1}{\Gamma(a)} 
b ^ a
\lambda ^ {a - 1}
\exp (- b \lambda).
\end{equation}
%
Then
%
\begin{equation}
{\rm E} \lambda
=
\frac{1}{\Gamma(a)}
b ^ a
\int_{0}^{\infty}
\lambda ^ a
\exp
\left(
	-
	\frac{\lambda}{b}
\right)
d\lambda.
\end{equation}
%
By the transformation
%
\begin{equation}
\lambda'
= 
b \lambda,
\end{equation}
%
the right hand side can be written as
%
\begin{equation}
\frac{1}{\Gamma(a)} 
b ^ a
\int_{0}^{\infty}
\left(
	\frac{\lambda'}{b}
\right) ^ a
\exp (- \lambda')
\frac{1}{b}
d\lambda'
=
\frac{1}{b \Gamma(a)}
\int_{0}^{\infty}
{\lambda'} ^ a
\exp (- \lambda')
d\lambda'.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\frac{1}{b \Gamma(a)}
\Gamma(a + 1)
=
\frac{a}{b}.
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm E} \lambda
=
\frac{a}{b}.
\end{equation}
%

Additionally,
%
\begin{equation}
{\rm E} \lambda ^ 2
=
\frac{1}{\Gamma(a)}
b ^ a
\int_{0}^{\infty}
\lambda ^ {a + 1}
\exp
\left(
	-
	\frac{\lambda}{b}
\right)
d\lambda.
\end{equation}
%
By the transformation
%
\begin{equation}
\lambda'
= 
b \lambda,
\end{equation}
%
the right hand side can be written as
%
\begin{equation}
\frac{1}{\Gamma(a)} 
b ^ a
\int_{0}^{\infty}
\left(
	\frac{\lambda'}{b}
\right) ^ {a + 1}
\exp (- \lambda')
\frac{1}{b}
d\lambda'
=
\frac{1}{b ^ 2 \Gamma(a)}
\int_{0}^{\infty}
{\lambda'} ^ {a + 1}
\exp (- \lambda')
d\lambda'.
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\frac{1}{b ^ 2 \Gamma(a)}
\Gamma(a + 2)
=
\frac{a (a + 1)}{b ^ 2}.
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm E} \lambda ^ 2
=
\frac{a (a + 1)}{b ^ 2}.
\end{equation}
%
By the definition,
%
\begin{equation}
{\rm var} \lambda
=
{\rm E} \lambda ^ 2
-
({\rm E} \lambda) ^ 2.
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm var} \lambda
=
\frac{a}{b ^ 2}.
\end{equation}
%

Finally, setting the derivative of ${\rm Gam}(\lambda | a, b)$ with respect to $\lambda$ to zero gives
%
\begin{equation}
0
=
\frac{1}{\Gamma(a)}
b ^ a
\left(
	\frac{a - 1}{\lambda}
	-
	b
\right)
\lambda ^ {a - 1}
\exp
\left(
	-
	\frac{\lambda}{b}
\right).
\end{equation}
%
Therefore,
%
\begin{equation}
{\rm mode} \lambda
=
\frac{a - 1}{b}.
\end{equation}
%


\subsection{}
\label{2.43}
Let
%
\begin{equation}
p
\left(
	x 
	| 
	\sigma ^ 2, 
	q
\right)
=
\frac{q}{2 \Gamma \left( \frac{1}{q} \right)} 
\left( 2 \sigma ^ 2 \right) ^ {- \frac{1}{q}}
\exp 
\left( 
-
\frac{\abs{x} ^ q}{2 \sigma ^ 2}
\right).
\end{equation}
%
Then
%
\begin{equation}
\int_{- \infty}^{\infty}
p
\left(
	x 
	| 
	\sigma ^ 2, 
	q
\right)
dx
=
\frac{q}{\Gamma \left( \frac{1}{q} \right)} 
\left( 2 \sigma ^ 2 \right) ^ {- \frac{1}{q}}
\int_{0}^{\infty}
\exp 
\left( 
-
\frac{x ^ q}{2 \sigma ^ 2}
\right)
dx.
\end{equation}
%
By the transformation 
%
\begin{equation}
x'
=
\frac{x ^ q}{2 \sigma ^ 2},
\end{equation}
%
the right hand side can be written as
%
\begin{equation}
\begin{aligned}
&
\frac{q}{\Gamma \left( \frac{1}{q} \right)} 
\left( 2 \sigma ^ 2 \right) ^ {- \frac{1}{q}}
\int_{0}^{\infty}
\exp ( - x' )
\left(
	2 \sigma ^ 2 
\right) ^ {\frac{1}{q}}
\frac{1}{q}
x ^ {\frac{1}{q} - 1}
dx' \\
&=
\frac{1}{\Gamma \left( \frac{1}{q} \right)}
\int_{0}^{\infty}
x ^ {\frac{1}{q} - 1}
\exp ( - x' )
dx'.
\end{aligned}
\end{equation}
%
The right hand side can be written as
%
\begin{equation}
\frac{1}{\Gamma \left( \frac{1}{q} \right)}
\Gamma \left( \frac{1}{q} \right)
=
1.
\end{equation}
%
Therefore,
%
\begin{equation}
\int_{- \infty}^{\infty}
p
\left(
	x 
	| 
	\sigma ^ 2, 
	q
\right)
dx
=
1.
\end{equation}
%

Additionally,
%
\begin{equation}
p
\left(
	x 
	| 
	\sigma ^ 2, 
	2
\right)
=
\frac{1}{\Gamma \left( \frac{1}{2} \right)}
\left(
	2 \sigma ^ 2
\right) ^ {- \frac{1}{2}}
\exp
\left(
	-
	\frac{x ^ 2}{2 \sigma ^ 2}
\right).
\end{equation}
%
Therefore,
%
\begin{equation}
p
\left(
	x 
	| 
	\sigma ^ 2, 
	2
\right)
=
\mathcal{N}
\left(
	x
	|
	0,
	\sigma ^ 2
\right).
\end{equation}
%

Finally, let $\mathbf{t} = (t_1, \cdots, t_N) ^ \intercal$ and $\mathbf{X} = \{ x_1, \cdots, x_N \}$ such that
%
\begin{equation}
t_n
=
y (\mathbf{x}_n, \mathbf{w})
+
\epsilon_n,
\end{equation}
%
where
%
\begin{equation}
p(\epsilon_n)
=
p
\left(
	\epsilon_n
	| 
	\sigma ^ 2, 
	q
\right).
\end{equation}
%
Therefore, the logarithm of $p(\epsilon_n)$ except the terms independent of $\mathbf{w}$ and $\sigma ^ 2$ can be written as
%
\begin{equation}
-
\frac{\abs{\epsilon_n} ^ q}{2 \sigma ^ 2}
-
\frac{1}{q}
\ln \left( 2 \sigma ^ 2 \right).
\end{equation}
%
Thus, the logarithm of $p\left( \mathbf{t} |\mathbf{X}, \mathbf{w}, \sigma ^ 2 \right)$ except the terms independent of $\mathbf{w}$ and $\sigma ^ 2$ can be written as
%
\begin{equation}
-
\frac{1}{2 \sigma ^ 2}
\sum_{n = 1}^{N}
\abs{y (\mathbf{x}_n, \mathbf{w}) - t_n} ^ q
-
\frac{N}{q}
\ln 
\left( 
	2 \sigma ^ 2
\right).
\end{equation}
%


\subsection{(Incomplete)}
\label{2.44}
Let $x_1, \cdots, x_N$ be variables such that
%
\begin{equation}
\begin{aligned}
p (x_n | \mu, \tau)
&=
\mathcal{N}
\left(
	x_n
	|
	\mu,
	\tau ^ {- 1}
\right),
\\
p(\mu, \tau)
&=
\mathcal{N}
\left(
	\mu
	|
	\mu_0,
	(\beta \tau) ^ {- 1}
\right)
{\rm Gam} (\tau | a, b).
\end{aligned}
\end{equation}
%
By the definition,
%
\begin{equation}
p(\mu, \tau | \mathbf{x})
p(\mathbf{x})
=
p(\mathbf{x} | \mu, \tau)
p(\mu, \tau).
\end{equation}
%
The right hand side except the terms independent of $\mathbf{x}$, $\mu$ and $\tau$ can be written as
%
\begin{equation}
\begin{aligned}
&
\tau ^ \frac{N}{2}
\exp 
\left(
	-
	\frac{\tau}{2}
	\sum_{n = 1}^{N} (x_n - \mu) ^ 2
\right)
\tau ^ \frac{1}{2}
\exp 
\left(
	-
	\frac{\beta \tau}{2}
	(\mu - \mu_0) ^ 2
\right)
\tau ^ {a - 1} 
\exp(- b \tau) \\
&=
\tau ^ {a + \frac{N - 1}{2}}
\exp
\left(
	-
	\frac{N \tau}{2}
	(\bar{x} - \mu) ^ 2	
	-
	\frac{\beta \tau}{2}
	(\mu - \mu_0) ^ 2
	-
	b \tau
\right)
\exp
\left(
	-
	\frac{\tau}{2}
	\sum_{n = 1}^{N} (x_n - \bar{x}) ^ 2	
\right).
\end{aligned}
\end{equation}
%
where
%
\begin{equation}
\bar{x}
=
\frac{1}{N}
\sum_{n = 1}^{N}
x_n.
\end{equation}
%
Since
%
\begin{equation}
\begin{aligned}
&-
\frac{N \tau}{2}
(\bar{x} - \mu) ^ 2	
-
\frac{\beta \tau}{2}
(\mu - \mu_0) ^ 2
-
b \tau \\
&=
-
\frac{(N + \beta) \tau}{2}
\left(
	\mu 
	-
	\frac{N \bar{x} + \beta \mu_0}{N + \beta}
\right) ^ 2
+
\left(
	-
	\frac{N \bar{x} ^ 2 + \beta \mu_0 ^ 2}{2}
	+
	\frac{(N \bar{x} + \beta \mu_0) ^ 2}{2 (N + \beta)}
	-
	b
\right)
 \tau \\
&=
-
\frac{(N + \beta) \tau}{2}
\left(
	\mu 
	-
	\frac{N \bar{x} + \beta \mu_0}{N + \beta}
\right) ^ 2
-
\left(
	\frac{N \beta (\bar{x} - \mu_0) ^ 2}{2 (N + \beta)}
	+
	b
\right)
\tau,
\end{aligned}
\end{equation}
%
the right hand side can be written as
%
\begin{equation}
\tau ^ {a + \frac{N - 1}{2}}
\exp
\left(
	-
	\frac{(N + \beta) \tau}{2}
	\left(
		\mu 
		-
		\frac{N \bar{x} + \beta \mu_0}{N + \beta}
	\right) ^ 2
	-
	\left(
		b
		+
		\frac{N \beta (\bar{x} - \mu_0) ^ 2}{2 (N + \beta)}
		+
		\frac{1}{2}
		\sum_{n = 1}^{N} (x_n - \bar{x}) ^ 2
	\right)
	\tau
\right).
\end{equation}
%
Therefore,
%
\begin{equation}
\begin{aligned}
&
p(\mu, \tau | \mathbf{x}) \\
&=
\mathcal{N} 
\left(
	\mu
	\left.
	\right \vert
	\frac{N \bar{x} + \beta \mu_0}{N + \beta},
	((N + \beta) \tau) ^ {- 1}
\right)
{\rm Gam}
\left(
	\tau
	\left.
	\right \vert
	a + \frac{N + 1}{2},
	b
	+
	\frac{N \beta (\bar{x} - \mu_0) ^ 2}{2 (N + \beta)}
	+
	\frac{1}{2}
	\sum_{n = 1}^{N} (x_n - \bar{x}) ^ 2
\right),
\end{aligned}
\end{equation}
%











